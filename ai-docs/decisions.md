# Журнал решений

Статусы: `принято`, `предварительно`, `отложено`, `отклонено`.

| ID | Дата | Статус | Решение | Обоснование |
|---|---|---|---|---|
| D-001 | 2026-08-24 | принято | Создавать Kilogram как самостоятельный открытый проект, а не форк Keet. | Полные исходники Keet и спецификация его протокола не опубликованы; проекту нужен собственный проверяемый протокол. |
| D-002 | 2026-08-24 | принято | Основное ядро писать на Rust. | Одно безопасное переносимое ядро для desktop, mobile, headless и relay; независимость от UI и Pear/Bare. |
| D-003 | 2026-08-24 | предварительно | Использовать Iroh как первый P2P transport. | Готовые QUIC, hole punching и зашифрованный relay fallback; решение требуется подтвердить desktop/mobile spike и сравнением с rust-libp2p. |
| D-004 | 2026-08-24 | принято | Использовать MLS RFC 9420 через OpenMLS для группового E2EE. | Не изобретать собственную групповую криптографию; OpenMLS является Rust-реализацией стандарта. Интеграционная модель ещё проектируется. |
| D-005 | 2026-08-24 | принято | Хранить историю как собственный подписанный event log / causal DAG. | Нужны проверяемая целостность, мультиустройство, P2P-репликация и независимость wire protocol от конкретной БД и сетевой библиотеки. |
| D-006 | 2026-08-24 | отклонено | Использовать блокчейн для порядка групповых сообщений. | Детерминированной линеаризации causal DAG и checkpoints достаточно; блокчейн добавляет задержку, Sybil-проблемы и сложность. |
| D-007 | 2026-08-24 | принято | На первом этапе разрешить direct P2P с раскрытием IP собеседнику. | Это упрощает MVP; риск должен быть явно отражён в продукте. |
| D-008 | 2026-08-24 | отложено | Добавить обязательный single-hop/multi-hop relay privacy mode. | Требуется позднее для сокрытия IP; доверенным контактам пользователь сможет разрешить direct P2P. |
| D-009 | 2026-08-24 | принято | Разрешить временное хранение только E2EE-ciphertext на случайных узлах. | Обеспечивает офлайн-доставку без выдачи владельцу узла ключей, содержимого и прикладных идентификаторов; metadata leakage полностью не исчезает. |
| D-010 | 2026-08-24 | принято | Разделить корневую идентичность аккаунта и отдельные ключи устройств. | Компрометация одного устройства не должна автоматически давать корневые полномочия и ключи остальных устройств. |
| D-011 | 2026-08-24 | предварительно | Обычные сообщения упорядочивать eventually-consistent; строгую финализацию применять к security-sensitive операциям. | Мгновенный глобальный total order конфликтует с доступностью P2P-системы при разделении сети. |
| D-012 | 2026-08-24 | принято | Hypercore, Autobase и HyperDHT рассматривать как референсы, не как выбранный фундамент. | Их идеи полезны, но основной вариант должен оставаться независимым от экосистемы Holepunch. |
| D-013 | 2026-08-25 | принято | Прикладная device identity не должна совпадать с transport identity Iroh. | Маршрут и transport могут меняться независимо от полномочий устройства; transport Endpoint ID не становится идентичностью аккаунта. |
| D-014 | 2026-08-25 | предварительно | Для M0 кодировать детерминированные signed events через Postcard, подписывать Ed25519 и идентифицировать BLAKE3. | Это минимальный проверяемый Rust-прототип с domain separation; межъязыковой публичный wire format и crypto agility ещё предстоит спроектировать. |
| D-015 | 2026-08-25 | предварительно | Для M0 хранить каждый signed event отдельным неизменяемым content-addressed файлом. | Это позволяет проверить append-only, deduplication, corruption detection и causal frontier до выбора production database; формат на диске и отсутствие at-rest encryption не являются production-решением. |
| D-016 | 2026-08-25 | предварительно | В M0 listener явно задаёт один `allowed requester device`; signed ticket связывает его с listener key и transport Endpoint ID. Sync дополнительно требует, чтобы requester уже был автором conversation; inventory и signed diff session-bound. | Обладатель ticket без разрешённого device key не может сначала внедрить событие, а затем получить историю. Обе стороны доказывают владение ключами до раскрытия events. Это временная граница до Account Root authorization, membership и revocation. |
| D-017 | 2026-08-25 | принято | Reconciliation state machine не должна зависеть от Iroh: session rules используют узкий store trait, а Iroh-specific ALPN и framing вынесены в отдельный adapter crate. | Проверки identity, session binding, exact event sets и continuation можно тестировать без сети; смена Iroh на другой transport не должна менять правила синхронизации. |
| D-018 | 2026-08-28 | предварительно | M0 connection ticket подписывает route policy `auto`, `direct-only` или `relay-only`. `relay-only` отключает IP transports; `direct-only` допускает relay для установления связи и NAT traversal, но блокирует прикладные frames до выбора direct path. | Так тест принудительного relay действительно исключает скрытый direct fallback, а проверка hole punching не ломается отключением relay-координации. Это transport-level M0 policy, а не окончательная модель privacy preferences. |
| D-019 | 2026-08-31 | отклонено | Считать pinning relay-only dialer к автоматически выбранному home relay listener достаточным исправлением. | Внешний retest подтвердил одинаковые `relay_home_url` и `target_relay_url` (`euc1`) на обоих endpoints, но handshake всё равно завершился timeout. Pinning сохраняется как корректная детерминизация маршрута, но сам по себе дефект не устраняет. |
| D-020 | 2026-08-31 | принято | Разрешить listener явно задавать единственный relay через `--relay-url`; URL попадает в подписанный ticket, а relay-only dialer использует тот же relay. | `auto` успешно выполнил внешний exchange через `aps1`, тогда как автоматически выбранный `euc1` дважды не доставил strict relay-only handshake. Явный `aps1` затем успешно перенёс strict relay-only exchange между теми же двумя сетями. Override нужен для детерминированного M0 и является прототипом согласованного privacy relay selection; production failover ещё предстоит спроектировать. |
| D-021 | 2026-08-31 | принято | В M0.4 использовать durable event set как checkpoint корректности sync после обрыва, не вводя отдельный переносимый cursor поверх full-ID inventory. | Полученные events сохраняются до подтверждения и идемпотентны; свежий session-bound inventory после reconnect вычисляет только остаток. Отдельный token пока не уменьшил бы полный inventory и потребовал бы решить staleness/snapshot semantics. Подписанный cursor проектируется вместе с compact Merkle/range summary. |
| D-022 | 2026-08-31 | принято | В M0.5.1 `AccountId` является отдельным Ed25519 public root key; root secret подписывает только device certificates и revocations. Валидный отзыв навсегда запрещает конкретный device key, поэтому повторное добавление требует нового ключа. | Прямая публичная проверка не требует key-resolution слоя; отделение root от device ограничивает полномочия обычного устройства; монотонный отзыв исключает двусмысленное повторное доверие уже скомпрометированному ключу. Seed/recovery, protected root storage и distributed authority log остаются отдельными решениями. |
| D-023 | 2026-08-31 | принято | M0.5.2 заменяет `--allow-device` и known-author на ticket v3 с listener certificate, allowed requester Account ID, явный expected peer Account ID и device-signed Endpoint-bound authorization proof до application data. Revocations проверяются из caller-supplied trusted files. | Новый сертифицированный device аккаунта может синхронизировать пустую историю без synthetic event; другой аккаунт, чужой key или известный revoked device отклоняются до event/inventory. Явный `--expect-account` не позволяет подменить весь ticket другим self-consistent account. Файлы отзывов дают проверяемую validity, но не freshness/completeness; authenticated authority-log distribution остаётся отдельным этапом. |
| D-024 | 2026-08-31 | принято | M0.6.1 публикует root-signed полный permanent-revocation set как `AccountAuthoritySnapshot(revision)`, передаёт его в ticket v4/session authorization v2 и сохраняет max-seen revision каждого peer account. Старые revision и разные snapshot одной revision отклоняются. | Snapshot доказывает completeness на конкретной root revision и устраняет ручные списки файлов. Anti-rollback действует после наблюдения новой revision, но без discovery/gossip/witness не доказывает глобальную freshness при первом контакте. Legacy root с authority operations без durable revocation log не может безопасно объявить snapshot полным и не мигрируется автоматически. |
| D-025 | 2026-08-31 | принято | В M0.6.2 один owner Account Root подписывает полный add-only `ConversationMembershipSnapshot`, а каждое сетевое/синхронизируемое событие переносит `DeviceCertificate` и `AccountAuthoritySnapshot` автора в `AuthorizedEvent`. Локальный membership является trust anchor и не принимается от peer автоматически. | Это даёт проверяемую цепочку membership → account → device → event и разрешает безопасно отклонять history автора вне разговора. Add-only честно откладывает removal до ordered security log и MLS epoch. Обязательный immutable authorization sidecar сохраняет стабильный Event ID, но делает старые M0 events без Account proof несовместимыми и немигрируемыми автоматически. |
| D-026 | 2026-08-31 | принято | M0.7.1 использует RFC 9180 HPKE Base mode X25519/HKDF-SHA256/ChaCha20-Poly1305 как pairwise payload baseline. Root-signed DeviceCertificate v2 связывает отдельный encryption public key, а каждый encrypted text event содержит canonical box для текущего sender device и одного peer device; plaintext-варианта в event schema нет. | Стандартизованный single-shot primitive позволяет проверить E2EE-композицию с signed DAG/store/sync без самодельной криптографии. Внешняя Ed25519 event signature аутентифицирует sender и весь ciphertext envelope. Решение намеренно не обещает forward secrecy/PCS и не заменяет будущие asynchronous prekeys/Double Ratchet или two-member MLS. |
| D-027 | 2026-08-31 | принято | M0.7.2 разделяет recipient-only replicated ciphertext event и local-only encrypted plaintext projection. Sender box удалён из event; projection адресуется Event ID, шифруется на локальный device key, immutable и никогда не передаётся sync. | Double Ratchet удаляет использованные message keys. Статический sender box в общем event позволил бы восстановить всю записанную исходящую переписку после компрометации sender key и тем самым отменил бы forward secrecy. Разделение сохраняет читаемую локальную историю и создаёт корректную storage boundary для следующего ratchet-этапа. Утрата sender projection требует будущего authenticated history rewrap, а не возврата sender box. |
| D-028 | 2026-08-31 | принято | M0.7.3 использует `vodozemac::olm` 0.10.0 за отдельной границей `kilogram-ratchet`: Device-signed Olm identity и single-use prekey входят в ticket v7, а `RatchetText` v4 хранит только peer Device ID, signed sender ratchet identity и PreKey/Normal ciphertext. Account и session pickles сохраняются зашифрованными; HPKE остаётся только у local projection. | Реальная проверенная реализация Double Ratchet даёт per-message key evolution без собственной криптографии и убирает static decrypt route из replicated log. Device signature связывает Matrix/Olm keys с существующей Account/Device authorization chain. Выбор остаётся M0-кандидатом: один OTK/одна session на device pair не решают concurrent initiation, fan-out, PQ, transactionality или recovery. |
| D-029 | 2026-08-31 | принято | Ratchet state является обязательной частью состояния устройства и не выводится повторно только из device signing key/seed. Sync может принять ещё не обработанные ciphertexts при сохранённой session, но не обещает расшифровать старую историю после потери Olm account/session или sender projection. | Forward secrecy несовместима с универсальной расшифровкой старого общего ciphertext долговременным seed-derived ключом. Восстановление должно быть отдельным авторизованным протоколом device fan-out/history rewrap с явной неполнотой, а production storage — транзакционно связывать ratchet, projection и event. |
| D-030 | 2026-09-01 | принято | M0.7.4 вводит `AccountDeviceListSnapshot`: Account Root подписывает полный канонический набор messaging device certificates на конкретной authority revision и не публикует другой набор на той же revision. `AccountPrekeyDirectory` требует точное one-to-one покрытие списка device-signed bundles; ticket v8 подписывает конкретный directory online listener. | Sender должен получить проверяемый исчерпывающий перечень получателей, а не доверять listener, который мог бы умолчать offline device. Root-signed completeness и exact coverage делают пропуск, outsider, duplicate и bundle substitution обнаружимыми. Ручная публикация certificate/bundle files остаётся M0 distribution boundary, а не гарантией first-contact freshness. |
| D-031 | 2026-09-01 | принято | Один `RatchetText` event v5 содержит embedded root-signed recipient device list и отдельный canonical Olm ciphertext для каждого Device ID из него. Exact slot coverage проверяется при любом decode/store/sync; local client дополнительно требует, чтобы список принадлежал его Account ID. Новый device не добавляется задним числом в старые events. | Один Event ID сохраняет общий causal DAG всех устройств, а отдельная pairwise session не раскрывает slot другого device. Embedded list позволяет независимо проверить полноту после потери исходного ticket. Старую историю следует передавать отдельным authenticated history-rewrap протоколом, иначе изменение immutable event разрушило бы подпись и content addressing. |
| D-032 | 2026-09-01 | принято | M0.7.5 history rewrap выполняется отдельным same-account bundle: live source device подписывает canonical source text-inventory digest и диапазон, включает исходные `AuthorizedEvent` и шифрует каждый plaintext на persistent encryption key нового device. Старые events, Event ID и ratchet ciphertext не изменяются. | Forward secrecy не допускает восстановления старой истории из seed/device key или добавления нового recipient slot задним числом. Отдельный signed HPKE transfer даёт явные source, target, range и provenance, а embedded event позволяет новому device импортировать history до обычного sync. Cross-account recovery требует отдельной trust/user-consent policy. |
| D-033 | 2026-09-01 | принято | Rewrapped local projection v2 сохраняет подписанные manifest и entry и при каждом чтении требует совпадения local Account ID, Device ID и immutable event. Direct projection v1 остаётся читаемой. `source_inventory_complete` означает только покрытие inventory, заявленного данным source, а не глобальную полноту разговора. | После импорта нельзя терять provenance и полагаться лишь на то, что когда-то CLI проверил bundle: public target encryption key позволяет любому создать ciphertext, поэтому source signature должна проверяться повторно. Глобальную полноту без authenticated gossip/checkpoint доказать нельзя; точное имя признака предотвращает ложное обещание recovery. |
| D-034 | 2026-09-01 | принято | M0.7.6 публикует `SignedPrekeyPool` с device-signed generation, непрерывным sequence range, publication/expiry и 1–64 independently consumable Olm OTK. Каждый observer сохраняет max-seen pool per Device ID и отклоняет generation rollback, same-generation equivocation, sequence/timestamp rollback и смену ratchet identity. | Один OTK не выдерживает несколько асинхронных first messages. Signed freshness metadata и local high-water делают replay измеримо обнаружимым после наблюдения новой версии, не выдавая first-contact freshness за глобальный consensus. Private OTK по-прежнему расходуется только после authenticated Olm decrypt; remote reservation/DHT остаются отдельной задачей. |
| D-035 | 2026-09-01 | принято | Для crossed pairwise initiation persistent session record v2 допускает active и максимум одну retained Olm session. Пока outbound не подтверждён, новый valid inbound PreKey создаёт второго кандидата; обе стороны выбирают lexicographic-min session ID как active, а losing session оставляют для уже летящих сообщений. После подтверждения неизвестный новый PreKey не сбрасывает ratchet. | В crossed X/Y обе стороны получают одинаковый набор session IDs, поэтому детерминированный выбор не требует coordinator и не теряет первые сообщения. Bounded retained session позволяет расшифровать запоздавшие пакеты losing ветви, но не открывает неограниченный session-reset/DoS route; отдельный authenticated reset и TTL нужны позже. |
| D-036 | 2026-09-01 | принято | M0.7.7 сериализует все device `STATE_DIR` commands exclusive OS file lock и использует один prepared journal: mutable `ratchet`/`next-sequence` копируются, а для immutable `events`/`local-messages`/`history-rewraps` фиксируется baseline. Prepared без terminal marker откатывается, `committed` сохраняется, `rolled-back` только очищается. | Продвижение ratchet, расход prekey, author sequence, projection и event должны переживать crash как одна логическая операция. Immutable noclobber stores позволяют безопасно удалить только новые файлы, не копируя всю историю. Snapshot/baseline остаётся M0 механизмом `O(local state)`; production должен перейти на encrypted transactional DB/WAL. |
| D-037 | 2026-09-01 | принято | M0.7.8 network history rewrap разрешён только same-account device pair из одного root-signed device list и требует role-bound 12-digit SAS, явно подтверждённый обеими сторонами. Recipient подписывает session-bound conversation/range request, source подписывает transfer поверх точного request и существующего encrypted rewrap bundle; ALPN повышен до `kilogram/m0/sync/7`. | Сам факт authenticated connection недостаточен для чувствительной передачи всей читаемой истории: ошибочно выбранный, подменённый или скомпрометированный device должен быть заметен пользователю до открытия projections. Двойная подпись связывает consent, текущую session и ciphertext provenance, не меняя старые events и не ослабляя ratchet forward secrecy. |
| D-038 | 2026-09-01 | принято | Reconciliation группирует signed rewrap claims по `(source Device ID, inventory count, digest)`, объединяет bounded ranges и различает `incomplete`, `single-source`, `agreed` и `divergent`; same-source разные claims считаются equivocation. Любой результат сопровождается `global_completeness_proven=false`. | Один source может быть отставшим или злонамеренным, а несколько sources могут независимо не знать утраченный event. Сравнение signed claims полезно для recovery UX и обнаружения расхождения, но без gossip/checkpoint не является blockchain, total order или доказательством глобальной полноты. |
| D-039 | 2026-09-01 | принято | M0.7.9 хранит для каждого явно выбранного source recipient-signed append-only checkpoint chain: Account/Conversation/roles/SAS/approved window/page size неизменяемы, первый transfer фиксирует source inventory claim, каждая следующая страница обязана продолжать точный индекс и прежний claim. Import страницы и нового checkpoint выполняется одной M0.7.7 transaction. | Session-bound request нельзя безопасно переносить между соединениями, поэтому новый request подписывается для каждого fresh ticket, а переносится только local progress. Hash-link обнаруживает пропуск/перестановку/fork сохранённой цепочки; полный local rollback/delete без внешнего witness остаётся недетектируемым. |
| D-040 | 2026-09-01 | принято | Source consent для network rewrap задаёт полное окно, внутри которого каждая страница всё ещё ограничена 256 events; один listener обслуживает одну страницу. Inventory выбирается автоматически только при `agreed`: минимум два полных явно собранных source claim имеют одинаковые count/digest и отсутствует equivocation. | Окно убирает ручное согласование каждого следующего индекса, не разрешая запрос за пределами одобренной истории. Отдельный fresh listener/ticket сохраняет явность и session binding в M0; agreed отражает только наблюдавшиеся sources и поэтому не меняет `global_completeness_proven=false`. |
| D-041 | 2026-09-01 | принято | M0.8.1 использует `redb` 4.2 как embedded transactional engine для теневого encrypted snapshot всего device state. Relative paths становятся keyed BLAKE3 lookup keys, а path+content шифруются XChaCha20-Poly1305; records и keyed manifest публикуются одной immediate-durability transaction. | Сначала нужно доказать атомарную, аутентифицированную и обратимую миграцию без destructive переключения live filesystem store. `redb` скрывается за `kilogram-state` и пока не считается необратимым production-выбором. |
| D-042 | 2026-09-01 | принято | Первый vault migration сохраняет legacy state, запрещает overwrite существующего vault при drift и восстанавливает только в новый каталог через проверенный staging+rename. Случайный master key пока хранится соседним `state-vault.key`. | Fail-closed shadow migration позволяет откатиться к прежнему M0 state и не угадывает победителя при расхождении. Соседний key file защищает только при отдельной утечке DB; OS keystore/passphrase/seed wrapping, rollback witness и secure deletion остаются обязательными отдельными этапами. |
| D-043 | 2026-09-01 | принято | M0.8.2 выполняет recoverable shadow dual-write через `StateMirrorRepository`: перед каждой live device-state командой vault immediate-transaction сохраняет keyed-authenticated intent к exact active generation/snapshot, после команды атомарно публикует новый encrypted snapshot и удаляет intent. | Filesystem и embedded DB не имеют общей atomic transaction. Intent позволяет после crash отличить легитимный незавершённый mirror от external drift: с валидным intent сначала завершается M0.7.7 recovery и затем mirror, без intent любое расхождение блокирует команду. |
| D-044 | 2026-09-01 | принято | Legacy filesystem остаётся primary read/write store M0.8.2; vault generation повышается только при изменении canonical state, а read-only command лишь подтверждает generation и очищает intent. | Full snapshot mirror пока `O(state)`, но безопасно проверяет live lifecycle и backward-compatible metadata upgrade vault M0.8.1. Typed incremental repositories и shadow-read equivalence обязательны до DB primary cutover; generation не является внешним rollback witness. |
| D-045 | 2026-09-01 | принято | M0.8.3 сравнивает active и legacy records по canonical relative path и одной immediate `redb` transaction применяет только encrypted upserts, keyed removals, manifest, следующее generation и intent removal. Неизменённые ciphertext values не переписываются. | DB encryption/write amplification становится `O(changed + deleted)`, сохраняя crash protocol M0.8.2. Полный filesystem scan, decrypt и exact comparison пока остаются `O(state)` ради fail-closed shadow equivalence. |
| D-046 | 2026-09-01 | принято | Device state разделён на девять стабильных `StateRecordKind`, включая явный `other`; `TypedStateRepository` сравнивает exact DB/legacy path+content и публикует per-kind inventory до любого primary-read cutover. | Неизвестные записи нельзя молча потерять, а mismatch локализуется по repository kind. Первый DB-primary canary допустим только для immutable event/projection adapters и обязан fail-closed без silent legacy fallback. |
| D-047 | 2026-09-01 | принято | M0.8.4 переключает только read-only `history` на DB-primary owned snapshot для `event` и `local-projection`; полный exact legacy shadow остаётся обязательным gate, а любая ошибка инициализированного vault завершает команду без filesystem fallback. | Узкий canary впервые доказывает использование DB bytes прикладным decoder, не смешивая cutover с live writes. Полностью отсутствующий vault сохраняет legacy compatibility; partial/invalid/drifted vault downgrade не разрешает. |
| D-048 | 2026-09-01 | принято | `kilogram-store` разделяет read interfaces от filesystem write stores: `EventReadRepository` и `LocalMessageReadRepository` реализованы legacy stores и strict owned immutable snapshots. Snapshot повторно проверяет canonical path, signatures, IDs, authorization/membership и projection binding. | Одинаковая прикладная validation применяется независимо от физического source. Mixed read/write sync нельзя переводить на snapshot начала команды без overlay/direct DB writes, иначе новые записи внутри команды могут быть невидимы. |
| D-049 | 2026-09-01 | принято | M0.8.5 переводит manual `history-rewrap-export` и explicitly approved network rewrap source на единый owned vault-primary event/projection snapshot. Listener захватывает его до authority/prekey/request operations; initialized vault при любой ошибке блокирует source без filesystem fallback. | История, передаваемая новому device, чувствительна и должна иметь тот же authenticated physical source, что и local history canary. Ранний owned snapshot сохраняет один immutable source view через async authorization и transport work, не смешивая его с command-local mutable state. |
| D-050 | 2026-09-01 | принято | `EventReadRepository: Send + Sync` предоставляет authorized inventory и events-by-ID как общий read-side contract, но M0.8.5 намеренно не переключает `SessionStore`/sync на snapshot. | Sync создаёт events/projections после начального inventory. До command-local overlay или direct transactional DB writes snapshot начала команды не увидит новые committed records и может дать stale subsequent round. |
| D-051 | 2026-09-01 | принято | M0.8.6 строит sync read view как authenticated immutable base плюс command-local committed event/projection overlay. Batch сначала staged без видимости, проходит M0.7.7 filesystem transaction и публикуется в overlay только после durable commit. | Это сохраняет один vault-primary snapshot через несколько network rounds, не допускает чтение rolled-back records и не требует преждевременного write-side DB cutover. |
| D-052 | 2026-09-01 | принято | Listener захватывает immutable read-set до определения application request, поэтому после инициализации vault canary выполняется для любой `listen` команды. | Тип request известен только после authority/prekey mutations и transport authorization, а sync base должен предшествовать этим изменениям. Один ранний snapshot одновременно покрывает возможный sync и approved history rewrap и сохраняет fail-closed semantics. |
| D-053 | 2026-09-01 | принято | M0.8.7 использует legacy tree как uncommitted staging внутри M0.7.7 journal, но необратимой точкой commit делает immediate redb delta transaction. Network result разрешён только после filesystem shadow commit и exact confirmation. | Сразу заменить все filesystem adapters слишком рискованно. Такой bridge сохраняет проверенный rollback до DB commit, а после DB commit однозначно назначает vault источником recovery. |
| D-054 | 2026-09-01 | принято | Vault-primary checkpoint включает полный transaction delta, в том числе ratchet и sequence, даже если текущий cutover мотивирован event/projection writes. | Event без соответствующего ratchet advancement либо acknowledgement без sequence нарушает криптографическую и writer-sequence согласованность; эти записи должны переживать crash как одна единица. |
| D-055 | 2026-09-01 | принято | Отдельный keyed `primary-shadow-intent-v1` атомарно публикуется с новой vault generation и остаётся до exact shadow confirmation. При crash marker заставляет восстановить legacy из DB; active filesystem journal блокирует recovery до штатного rollback/commit recovery. | Обычный M0.8.2 mirror intent разрешает legacy-to-DB repair и поэтому не различает направление авторитетности. Отдельный marker исключает откат уже committed primary snapshot к старому shadow. |
| D-056 | 2026-09-01 | принято | M0.8.8 direct vault transaction принимает typed write-set только от активного `StateTransaction`: ratchet/sequence сравниваются с journal backup, а append-only payload читается только у новых paths. | Full retained-tree payload scan больше не должен быть входом каждого primary commit. Связь с существующим crash journal сохраняет rollback до DB commit и не создаёт второй независимый transaction coordinator. |
| D-057 | 2026-09-01 | принято | Journal write-set повторно классифицирует canonical path и разрешает только ratchet, event, local-projection, history-rewrap, history-recovery и sequence. Trust/device/other остаются вне этого domain write API; append-only baseline removal запрещён. | Typed allowlist не даёт новому оптимизированному пути незаметно расширить authority или потерять неизвестный state. Trust repository получит отдельный явный cutover. |
| D-058 | 2026-09-01 | принято | `next-sequence` является первым mutable DB-primary canary: authenticated outer intent связывает read с active vault generation, локальный cursor поддерживает несколько allocation, filesystem получает только staged shadow. | Writer sequence мала, однозначна и участвует в той же transaction, что authored event/ack; она позволяет доказать mutable cutover без преждевременного рефакторинга сложного ratchet repository. |
| D-059 | 2026-09-01 | принято | Пока authority/contact code пишет trust вне `StateTransaction`, direct coordinator перед journal delta включает в ту же DB transaction только bounded trust namespaces и запрещает исчезновение уже committed trust record. | Реальный process smoke выявил trust update между journal transactions: без bridge exact shadow confirmation корректно останавливала команду. Bounded ingress сохраняет атомарность текущего CLI и не возвращает полный state scan; trust reads/domain writes всё ещё не считаются DB-primary. |
| D-060 | 2026-09-01 | принято | M0.8.9 читает ratchet records из authenticated active vault generation, гидратирует ими journaled staging до открытия `RatchetState` и строит mutation относительно DB baseline. | vodozemac пока файловый, поэтому staging bridge даёт DB-primary read semantics без рискованной замены crypto crate. Retained drift не становится входом encrypt/decrypt/prekey операции. |
| D-061 | 2026-09-01 | принято | После подготовки ratchet/sequence workspace crash journal атомарно переключается на отдельный durable primary backup; rollback восстанавливает DB snapshot, а не предкомандный filesystem shadow. | Иначе operation error мог бы вернуть tampered shadow, который внешний dual-write completion затем ошибочно принял бы за новый state. Отдельный backup сначала публикуется в manifest, а live shadow меняется только после этого. |
| D-062 | 2026-09-01 | принято | Append-only writers регистрируют canonical paths в `StateTransaction`; direct delta читает только этот write-set и больше не выполняет второй directory walk. | Delivery/connect/sync/history/seed точно знают создаваемые paths. Начальный baseline остаётся для crash rollback, а final exact confirmation ловит пропущенную регистрацию до успешного завершения команды. |
| D-063 | 2026-09-01 | принято | Зарегистрированный path, уже существующий в authenticated DB, может быть только byte-identical; modification-in-place и baseline removal отклоняются. | Явная регистрация не должна превращать immutable event/projection/history namespace в update API. New record разрешён, idempotent rewrite остаётся unchanged delta. |
| D-064 | 2026-09-01 | принято | Append-only domain writer возвращает `AppendOnlyWriteReceipt` с exact canonical paths после insert или byte-exact idempotent validation; coordinator принимает receipt только внутри своего canonical state root и повторно классифицирует kind. | CLI не должен дублировать repository extensions/layout: иначе изменение writer path может пропустить record в vault delta. Receipt другого state tree или unsafe kind обязан завершаться fail-closed. |
| D-065 | 2026-09-01 | принято | Vault schema v2 хранит AEAD-encrypted sorted manifest index `(path, plaintext length, content hash)` и вычисляет keyed snapshot ID по этому index. Typed primary commit обновляет index/manifest из journal delta и не загружает unchanged DB payload records. | Atomic DB writes уже были `O(delta)`, но full decrypt/re-hash active records сохранял read/crypto amplification. Аутентифицированные per-record commitments позволяют проверить append immutability и построить новый snapshot только по metadata плюс changed payload. |
| D-066 | 2026-09-01 | принято | Schema-v1 vault проверяется прежним алгоритмом и один раз перестраивается в schema v2; record envelope AAD остаётся v1-compatible. Schema-v2 missing/tampered index не перестраивается автоматически. Final exact DB/index/shadow confirmation остаётся обязательной до command success. | Upgrade не должен переписывать unchanged ciphertexts или принимать повреждённую schema-v2 DB за migration input. Отказ от pre-commit payload scan означает, что unrelated ciphertext tamper может обнаружиться только на final gate; primary-shadow marker сохраняет fail-closed recovery. |
| D-067 | 2026-09-01 | принято | Authority/contact trust reads используют отдельный `TrustStateRepository`: schema-v2 manifest index аутентифицируется целиком, а payload decrypt выполняется только для выбранных Trust records. Initialized vault не делает silent filesystem fallback при missing/invalid trust record. | Certificate, authority pin и membership являются trust anchors; retained shadow не должен становиться источником доверия после DB-primary cutover. Selected lookup уменьшает payload I/O без ослабления AEAD/path/hash проверки выбранных records. |
| D-068 | 2026-09-01 | принято | Каждый trust write обязан явно подготовить `StateTransaction` workspace из DB-primary baseline; durable primary backup, typed Trust delta, rollback и next-start recovery используют те же commit barriers, что ratchet/sequence. | Domain anti-rollback/equivocation/add-only validators можно сохранить, направив их в byte-exact staging, но filesystem mutation не должна обходить crash journal или становиться DB input неявно. |
| D-069 | 2026-09-01 | принято | Direct transaction больше не перечисляет retained trust namespaces. Только mutations подготовленного trust workspace могут менять DB; незарегистрированная filesystem подмена игнорируется direct pre-commit, явно отклоняется compatibility checkpoint/final mirror и дополнительно обнаруживается exact shadow gate. | M0.8.8 bounded ingress был временным мостом и позволял command coordinator принимать authority update без явного repository operation. Удаление моста делает mutation authority однозначной и проверяемой. |
| D-070 | 2026-09-01 | принято | `state-vault.key` становится versioned envelope; Windows provider заворачивает случайный 256-bit master key через DPAPI CurrentUser и сохраняет только ciphertext. Provider и load outcome выводятся в CLI; non-Windows явно остаётся `plaintext-development`. | DPAPI убирает raw key с диска без отдельного интерактивного секрета и привязывает открытие к Windows user/machine. Это защита at rest, а не защита от процесса, уже исполняющегося под тем же пользователем; portable provider остаётся отдельной задачей. |
| D-071 | 2026-09-01 | принято | Ровно 32-byte legacy key перед открытием DB автоматически rewrap-ится в same-directory temporary file, fsync и atomic replace; сам master key и vault ciphertext не меняются. Invalid magic/version/provider, tampered DPAPI blob и missing key fail-closed; raw-file secure erase не обещается. | Пользователь не должен вручную переencrypt-ить существующий vault, но crash не должен оставлять частично записанный envelope или разрешать downgrade. Secure deletion на SSD/журналируемой FS нельзя честно гарантировать обычной replace-операцией. |
| D-072 | 2026-09-01 | принято | Portable vault-key recovery использует внешний bounded no-clobber package с Argon2id v0x13 (`m=65536 KiB,t=3,p=1`) и XChaCha20-Poly1305; passphrase поступает только из bounded file, а точные KDF parameters входят в AEAD AAD. | DPAPI envelope намеренно непереносим. Явный package возвращает переносимость, не записывая raw master key и не разрешая attacker-selected KDF downgrade/DoS; package и passphrase вместе остаются полным секретом vault. |
| D-073 | 2026-09-01 | принято | Recovery plaintext связывает master key с `(schema,generation,snapshot_id)`. Import сначала candidate key полностью аутентифицирует DB, отклоняет `db_generation < witness_generation` и same-generation fork, затем атомарно устанавливает local provider envelope. | Ошибочный recovery не должен менять рабочий key file. Внешний package обнаруживает независимый DB rollback, но не согласованный rollback DB вместе со старой копией package; настоящий monotonic multi-device/hardware witness остаётся отдельным этапом. |
| D-074 | 2026-09-01 | принято | После инициализации vault все production-команды получают device signing/encryption secrets только через immutable `DeviceIdentityStateRepository`: authenticated manifest/index/generation, selected decrypt двух exact 32-byte records и никакого filesystem fallback. | Device certificate и authority checks бесполезны, если сама identity может быть незаметно взята из подменённого raw shadow. Missing, malformed или corrupt DB record должен завершать команду fail-closed. |
| D-075 | 2026-09-01 | принято | Device identity остаётся неизменяемой в direct transaction API; M0.8.14 меняет только read authority. Raw identity shadow и его exact pre-command/crash-recovery semantics удаляются отдельным следующим этапом. | Ротация device key является новой authority-операцией, а не обычной state mutation. Разделение read cutover и физического shadow retirement сохраняет проверяемую recovery-границу и не создаёт ложного обещания улучшенной at-rest confidentiality до фактического удаления plaintext copies. |
| D-076 | 2026-09-02 | принято | Vault schema v3 является authenticated layout marker: две `DeviceIdentity` records остаются в общем snapshot/index, но исключаются из normal retained filesystem shadow. Upgrade сначала публикует schema/generation в redb и только затем удаляет byte-exact raw copies. | Старый binary должен fail-closed на неизвестной schema, а crash между DB commit и удалением может оставить только лишнюю копию, но не потерять identity. Missing raw copy не означает удаление DB record. |
| D-077 | 2026-09-02 | принято | Schema-v3 exact gate строит effective snapshot из filesystem non-identity shadow плюс immutable DB identity records; primary-shadow recovery никогда не восстанавливает raw identity. Matching copy безопасно удаляется, mismatched copy не импортируется и блокирует команду. | Full compatibility coordinator пока нужен для остальных kinds, но он не должен возвращать filesystem authority для уже выведенных из shadow секретов. |
| D-078 | 2026-09-02 | принято | M0.9.1 разрешает до 64 смежных `HistoryRewrap` streams в одном уже авторизованном Iroh connection. Source сохраняет один immutable snapshot и exact consent window; recipient после каждой страницы отдельно проверяет transfer и атомарно коммитит новый signed checkpoint. | Устранение ручного ticket на каждую страницу не должно ослаблять Device ID/SAS consent, смешивать source inventory views или превращать многостраничный transfer в одну неатомарную transaction. Hard cap и resumable pause ограничивают ресурсы и сохраняют crash recovery. |
| D-079 | 2026-09-02 | принято | CLI command coordinator запускается в отдельном потоке со стеком 8 MiB; wire/runtime workers не меняются. | Расширенный debug async coordinator превысил default Windows main-thread stack ещё до dispatch. Явная platform-neutral stack boundary сохраняет диагностическую debug-сборку и не влияет на protocol state. |
| D-080 | 2026-09-02 | принято | M0.9.2 recovery link использует versioned Postcard/base64url URI и включает endpoint, source certificate, root-signed device list, exact recipient/conversation/range/page/route и expiry, но исключает все prekey pools. URI ограничен 2953 bytes, действует не более часа и подписан exact source device. | Полный ticket v9 около 36 KiB не помещается в QR главным образом из-за account-wide prekey pools. Они не нужны для history recovery bootstrap; компактный публичный descriptor сохраняет проверяемую authority и план, не превращая provisional M0 codec в окончательный wire standard. |
| D-081 | 2026-09-02 | принято | Inspect проверяет recovery link offline; accept до сети требует exact local recipient, совпадающий Conversation ID и вручную подтверждённый role-bound SAS. После подключения listener повторно выполняет device authentication и независимо применяет local consent window. | Signed discovery coordinates не являются bearer capability и не дают автоматического доверия найденному peer. Разделение inspect/accept делает пользовательское согласие наблюдаемым и не ослабляет прежние session-bound request/transfer/checkpoint проверки. |
| D-082 | 2026-09-02 | принято | Recovery QR renderer использует standard Version 1..40 QR, EC level L, обязательную quiet zone 4 modules и 4 pixels/module; PNG публикуется через same-directory temporary file с no-clobber persist. | EC L позволяет гарантированно вместить весь уже принятый 2953-byte URI limit в один Version 40 symbol. No-clobber не даёт повторному запуску незаметно заменить уже переданный endpoint/plan под тем же filename. |
| D-083 | 2026-09-02 | принято | QR input ограничен PNG/JPEG file до 16 MiB и 4096×4096, image allocation budget 64 MiB, ASCII payload до 2953 bytes и ровно одним detected QR; затем всегда запускается прежний signed-link verifier. | Image parser и detector являются новым недоверенным input boundary. Bounds уменьшают resource abuse, exact-one исключает ambiguous UI choice, а QR ECC не заменяет Root/source signatures, expiry, exact recipient/conversation и SAS. |
| D-084 | 2026-09-02 | принято | M0.9.4 LAN publication является отдельным opt-in listener flag и передаёт без нового envelope точную короткоживущую recipient-specific URI на `239.255.75.71:45371` с TTL 1 каждые 750 ms; loopback copy поддерживает same-host client/test. | Descriptor раскрывает LAN-наблюдателям публичные account/device/conversation/endpoint metadata. Default-off и ограниченная область честно отделяют локальное удобство от будущего global privacy-preserving discovery. Уже подписанный payload не создаёт вторую схему authority. |
| D-085 | 2026-09-02 | принято | Discovery является bounded read-only preflight: exact recipient/conversation/membership, expiry, optional source и local authority anti-rollback проверяются без Iroh connection. Только один не достигший cap candidate может быть сохранён no-clobber; несколько не выбираются автоматически, а SAS/accept остаются отдельным явным действием. | Наличие signed descriptor доказывает source authorization, но не намерение recipient и не является bearer capability. Разделение discovery и accept не позволяет фоновому multicast автоматически открыть историю или расширить trust. |
| D-086 | 2026-09-02 | принято | M0.9.5 persistent retry consent представлен отдельным recipient-signed plan v1 с account device list, source/recipient/SAS, conversation, range/page/route, execution policy и expiry до 168 часов. Plan создаётся no-clobber только после exact-device/membership/authority/SAS preflight без connection. | Автоматическое использование любого найденного descriptor смешало бы discovery с consent. Подпись recipient делает точные границы ранее данного разрешения проверяемыми, а короткий expiry и удаление plan file дают bounded local cancellation. |
| D-087 | 2026-09-02 | принято | Scheduler может заменить только ephemeral endpoint/timestamps свежей source-signed URI; device list, source/recipient, полный SAS, conversation, range, page size и route policy обязаны точно совпасть с approved plan. Ноль или несколько matching descriptors не приводят к connection. | Source restart неизбежно меняет transport Endpoint ID, но не должен неявно расширять authority, объём истории или privacy route. Новая authority revision требует нового human approval, даже если Root signature корректна. |
| D-088 | 2026-09-02 | принято | Plan runner не держит общий device-state lock и vault mirror intent во время LAN discovery и retry sleep. Lock берётся заново только для local preflight, active recovery attempt и checkpoint check. Network/power context в CLI явно caller-supplied; mobile/unknown default-deny. | Background wait не должен блокировать foreground messenger. State mutation всё ещё эксклюзивна и crash-consistent, а platform client позже заменит ручной context на trusted OS adapters и настоящий scheduler. |
| D-089 | 2026-09-02 | принято | M0.9.6 хранит retry state как recipient-signed append-only chain под `history-recovery/scheduler/<plan-id>`: exact Plan/Device ID, generation, previous State ID, lifecycle, counters, clock high-water и deadline. Record atomic no-clobber, до 4 KiB; loader требует непрерывную цепочку до 4096 records. | Перезаписываемый JSON не дал бы проверяемой restart semantics и позволил бы незаметно менять counters/deadline. Подпись и hash chain обнаруживают modification/fork/gap, а короткий state lock и vault dual-write сохраняют foreground concurrency и crash recovery. |
| D-090 | 2026-09-02 | принято | Перед discovery записывается bounded attempt lease; failure использует exponential equal-jitter между половиной cap и cap, где cap ограничен 3600 s. Wall-clock ниже signed `last_observed` блокирует выполнение; истёкший lease сначала превращается в signed failure. | Lease не допускает два coordinator процесса для одного plan и предотвращает немедленный crash loop. Jitter разводит retry storm, а fail-closed clock high-water не позволяет переводу часов назад приблизить попытку. Whole-directory rollback по-прежнему требует внешнего witness. |
| D-091 | 2026-09-02 | принято | `history-recovery-plan-cancel` добавляет terminal recipient-signed transition, допустимый даже после plan expiry и не выполняющий discovery/connection. Terminal state необратим для данного Plan ID; новый запуск требует нового human-approved plan. | Удаление одной копии plan file не отменяет уже скопированный artifact. Явное подписанное событие наблюдаемо, переживает restart и может остановить runner между discovery и connect без ослабления consent boundary. |
| D-092 | 2026-09-02 | принято | M0.9.7 вводит platform-neutral `RecoveryPlatformContextProvider`; Windows implementation использует WinRT Connectivity/Power только внутри CLI target adapter. `history-recovery-plan-run` без context flags выбирает native provider, а прежние manual network/power значения допустимы только полной парой. | Signed plan и protocol/core не должны зависеть от Win32 типов. Native default устраняет доверие к обычному caller, сохраняя deterministic compatibility path для process tests и будущую заменяемость macOS/Linux/mobile providers. |
| D-093 | 2026-09-02 | принято | Windows network probe признаёт только exact WLAN/WWAN/IANA Ethernet types. Tunnel/VPN profile может быть заменён только единственным active physical profile; ambiguity, неизвестные cost или roaming дают `unknown`. Fixed/variable cost и roaming отображаются в signed `mobile` policy bucket. | Угадывание Ethernet по любому non-Wi-Fi профилю могло разрешить большую recovery transfer через VPN/mobile/metered route вопреки consent. Консервативное агрегирование и существующий default-deny unknown/mobile сохраняют fail-closed policy без plan-format migration. |
| D-094 | 2026-09-02 | принято | Windows power probe использует WinRT `PowerManager`: external требует `Adequate`, battery требует discharging/idle, остальные комбинации дают unknown. Diagnostics не сохраняют и не печатают profile name, SSID, adapter GUID или IP. | Platform telemetry нужна для policy, но не должна создавать новый локальный metadata log или раскрывать сетевые идентификаторы в support output. Разовый snapshot остаётся минимальным срезом до background service/change subscriptions. |
| D-095 | 2026-09-02 | принято | M0.9.8 вводит отдельный `history-recovery-plan-watch` как bounded foreground process: runtime и meaningful wakeups имеют hard caps, cancellation polling не считается wakeup, а Windows service/Task Scheduler автоматически не устанавливается. | Event-driven retry нужен до GUI, но бесконечный daemon и неявное изменение OS state расширили бы authority и lifecycle среза. Ограниченный процесс можно безопасно проверить и позже явно зарегистрировать platform installer-ом. |
| D-096 | 2026-09-02 | принято | Windows worker подписывается на WinRT NetworkStatusChanged и PowerManager supply/battery/EnergySaver changes через process-local sequence/Notify; RAII снимает tokens. Scheduler deadline и внешний signed State ID являются отдельными wake sources. | Callback не должен выполнять state/network работу или логировать сетевые идентификаторы. Sequence с no-lost-wakeup registration сводит callback к сигналу, а signed deadline остаётся authority для времени следующей попытки. |
| D-097 | 2026-09-02 | принято | Signed execution policy повторно проверяется native provider-ом непосредственно перед attempt lease/discovery и перед connect после unique descriptor. Pre-discovery block не создаёт lease/UDP; pre-connect block не открывает connection и завершает started lease signed failure. | Сеть или питание могут измениться во время живого процесса. Однократный snapshot позволял бы начать дорогой или запрещённый transfer после смены context; двойной gate закрывает TOCTOU до обеих внешних операций. |
| D-098 | 2026-09-02 | принято | Worker не держит state lock во время ожидания и повторяет только typed `StateError::AlreadyLocked` с bounded двухсекундным ожиданием. Подписанный terminal state проверяется даже после plan expiry, но non-terminal attempt после expiry запрещён. | Cancel и worker poll неизбежно могут кратко столкнуться на lock; fail-fast сделал бы корректную отмену причиной падения worker. Typed bounded retry не маскирует другие I/O ошибки, а terminal-after-expiry нужен только для безопасного прекращения authority, не для его продления. |
| D-099 | 2026-09-02 | принято | Worker runtime ограничивает также активный runner future. При timeout future прекращается, а оставшийся signed Attempting lease в bounded cleanup немедленно записывается как failure; terminal state сохраняет приоритет. | Ограничение только wait loop не ограничивало бы процесс при длинном discovery/connect/page timeout. Простое drop без scheduler reconciliation оставило бы lease до его далёкого expiry и задержало безопасный retry. |
| D-100 | 2026-09-02 | принято | M0.9.9 `runtime` сохраняет один Iroh Endpoint ID и ticket на весь process lifetime, но заново выполняет device/account authorization для каждого connection и изолирует session error без остановки listener. | Стабильный endpoint убирает one-shot ceremony, а повторная криптографическая авторизация не превращает долгоживущий transport address в bearer capability. Ошибочный сетевой клиент не должен лишать пользователя доступности. |
| D-101 | 2026-09-02 | принято | Runtime не использует outer state lock/vault intent: accept и route wait идут без lock, одна application session получает bounded typed-lock retry и отдельный vault dual-write; sessions выполняются последовательно. | Блокировка на весь срок процесса сделала бы foreground send/UI невозможными. Короткая transaction сохраняет single-writer ratchet/sequence invariant; параллельные state mutations требуют будущего actor/API, а не незащищённого обхода lock. |
| D-102 | 2026-09-02 | принято | Runtime ticket публикуется через same-directory temporary file, fsync и atomic replace; restart обязан создать новый endpoint/ticket. OS autostart/background registration остаётся optional настройкой, а не обязательным M1 этапом. | Peer не должен прочитать частичный descriptor, но старый endpoint нельзя обещать после process restart без отдельной persistent transport-key политики. Запущенное приложение само хостит runtime; закрытое приложение по пользовательскому ожиданию не обязано работать в фоне. |
| D-103 | 2026-09-03 | принято | M0.9.10 contact — device-signed append-only карточка local/peer account, exact peer device, conversation, route и canonical path атомарно обновляемого public runtime ticket. Descriptor path должен быть вне protected state. | Endpoint ID меняется при restart, поэтому addressing отделён от доверенного contact metadata. Exact device/route fail closed при неожиданной ротации; file-path — только M0 adapter до authenticated discovery/gossip. |
| D-104 | 2026-09-03 | принято | Queue body немедленно HPKE-шифруется локальному device; queue/materialization/delivery/retry records подписаны отдельными domains и входят в новый append-only typed `Runtime` vault kind. | Outbox обязан переживать restart, не оставляя plaintext и не создавая неучтённый filesystem ingress рядом с DB-primary state. Раздельные domains и deterministic filenames связывают content с ролью и Queue ID. |
| D-105 | 2026-09-03 | принято | Runtime материализует Queue ID ровно в один полный `AuthorizedEvent` transaction и при retry передаёт тот же event; listener возвращает существующий ACK при replay. ACK и delivered marker sender коммитит вместе. | Обрыв после remote commit, но до local ACK commit иначе создавал бы новое ratchet сообщение/sequence или несколько ACK. Persisted full event и idempotent receiver дают exactly-once visible text поверх at-least-once network retry. |
| D-106 | 2026-09-03 | принято | Failed delivery создаёт signed hash-linked retry generation с bounded exponential equal-jitter; runtime polling сначала обслуживает due outbox, затем periodic contact sync. State mutations последовательны, network wait проходит без sender state lock. | Retry deadline должен пережить restart и не превращаться в tight loop. Приоритет delivery уменьшает latency, automatic sync обеспечивает convergence, а single writer сохраняет sequence/ratchet invariant до actor IPC. |
| D-107 | 2026-09-03 | принято | Iroh accept future сохраняется между runtime polling ticks и пересоздаётся только после принятого connection; Ctrl+C future также не отменяется каждым tick. | Drop незавершённого Iroh Incoming отклоняет peer handshake. Пересоздание accept на каждом poll делало исходящую очередь причиной случайного отказа входящих соединений. |
| D-108 | 2026-09-03 | принято | M0.9.11 local API вынесен в reusable `kilogram-runtime-ipc`: ephemeral loopback TCP, bounded Postcard framing и device-signed atomic descriptor с random bearer token. | GUI должен разделять один versioned contract с runtime и не копировать stdout/state layout. Loopback не открывает remote API; подпись обнаруживает field tamper, а защита от полной same-user подмены требует pin/OS peer credentials и остаётся вне M0 boundary. |
| D-109 | 2026-09-03 | принято | Connection tasks только проверяют framing/token и передают request через bounded MPSC; business/state command выполняется последовательно главным runtime actor. | Медленный local socket не должен блокировать Iroh accept, но sequence/ratchet/vault mutations нельзя распараллеливать без нового transaction design. |
| D-110 | 2026-09-03 | принято | QueueMessage несёт persistent 256-bit request ID, который становится Queue ID; exact replay idempotent, reuse для другого content отклоняется. | Потерянный IPC response не должен создавать второе видимое сообщение при безопасном retry UI. |
| D-111 | 2026-09-03 | принято | Bearer descriptor хранится только в private machine-local path вне protected state и удаляется compare-before-delete. Same-user malware не входит в M0 guarantee; OS peer credentials отложены. | Token нельзя зеркалировать в vault или синхронизировать между машинами. Compare-before-delete не даёт старому runtime удалить descriptor нового instance. |
| D-112 | 2026-09-03 | принято | M0.9.12 desktop GUI является отдельным safe-Rust `kilogram-windows` package поверх `kilogram-runtime-ipc`; он не зависит от state/store/session/transport и выполняет IPC в одном worker thread. | Оконный event loop не блокируется bounded network timeout, а runtime остаётся единственным state writer. `eframe` 0.33.3 сохраняет workspace MSRV 1.91 и оставляет возможность общего desktop UI для Windows/macOS/Linux. |
| D-113 | 2026-09-03 | принято | GUI допускает одну IPC operation за раз, опрашивает outbox не чаще одного раза в 2 секунды и удерживает QueueMessage request ID после неопределённой ошибки, пока draft не изменён. | Bounded polling достаточно до push revision; сохранённый request ID использует M0.9.11 idempotency и не превращает потерянный локальный response в дублирующее видимое сообщение. |
| D-114 | 2026-09-03 | принято | IPC v2: conversation summaries и readable history формирует только runtime actor из проверенных contact/trust/event/local-projection snapshots под state lock; GUI получает plaintext исключительно через authenticated loopback IPC. | Новый descriptor version не даёт GUI принять старый runtime без read capability. UI не должен становиться вторым state reader/writer или загружать device keys; actor-owned read model сохраняет DB-primary, E2EE и single-writer границы. |
| D-115 | 2026-09-03 | принято | Private-chat projection использует causal topological order с tie-break `(author_sequence, author_device_id, event_id)` среди одновременно готовых событий. | Одинаковый event set даёт одинаковый UI order и всегда соблюдает parent-before-child, но это явно не group consensus и не заменяет будущий MLS/governance ordering. |
| D-116 | 2026-09-03 | принято | History IPC ограничен 100 text events и 192 KiB body на страницу; cursor связывает exclusive index с BLAKE3 digest упорядоченных text Event IDs и устаревает при изменении text snapshot. | Одного count limit недостаточно при сообщениях по 64 KiB. Snapshot-bound cursor не допускает незаметно смешать страницы разных локальных views, а ACK не должен инвалидировать видимую pagination. |
| D-117 | 2026-09-03 | принято | Первый periodic contact sync становится due только после полного `auto_sync_seconds` от старта runtime; due outbox по-прежнему проверяется первым на каждом tick. | Немедленный sync при пустом in-memory last-attempt map мог занять peer и исчерпать bounded action/session budget во время локального GUI bootstrap до постановки сообщения. Interval должен означать период, а не startup action. |
| D-118 | 2026-09-03 | принято | IPC v3 добавляет `AddContact` и `Shutdown`; обе команды аутентифицируются bearer descriptor, а contact mutation выполняется главным runtime actor под коротким state lock и vault dual-write. | GUI не должен читать peer ticket, membership, trust или vault напрямую. Runtime повторно проверяет expected Account ID, certified Device ID, membership и route; shutdown проходит обычный cleanup вместо принудительного завершения процесса. |
| D-119 | 2026-09-03 | принято | Desktop runtime запускается через versioned no-clobber launch-profile с абсолютными путями и public settings; profile и private IPC descriptor находятся вне protected state и не содержат seed/device/vault/bearer secrets. | Передача длинного набора CLI flags из UI хрупка, а копирование runtime логики создало бы второго владельца state. `runtime-from-profile` использует единственную существующую runtime реализацию и заново валидирует профиль при каждом старте. |
| D-120 | 2026-09-03 | принято | GUI владеет только явно запущенным им foreground child runtime: bounded readiness retry, authenticated stop, graceful close и hard-kill лишь как timeout fallback; OS autostart/service не регистрируется. | Это соответствует ожиданию, что закрытый messenger не работает в фоне, не вносит скрытых OS mutations и сохраняет возможность отдельно подключиться к уже запущенному runtime. |
| D-121 | 2026-09-03 | принято | IPC v4 `WaitForChange` обслуживается authenticated loopback connection task из in-memory watch revision и не передаётся в actor MPSC; ожидание ограничено 25 секундами, timeout повторно читает revision. | Long poll не должен блокировать единственного state writer или bounded command queue. Revision является только wake-up hint одного runtime lifetime, а повторная проверка закрывает race между timeout и публикацией. |
| D-122 | 2026-09-03 | принято | Runtime публикует change revision только после committed/meaningful contact, queue, delivery/retry, sync или inbound-session результата; GUI coalesces wake-ups и заново читает actor-owned conversations → history → outbox. | Уведомление не является domain event или источником истины. Snapshot reread сохраняет DB-primary/cryptographic validation, убирая unconditional двухсекундный polling без потери single-operation UI boundary. |
| D-123 | 2026-09-03 | принято | Desktop может атомарно заменить secret-free launch-profile только при остановленном/disconnected runtime; shared profile validation canonicalizes inputs и проверяет runtime interval bounds. | Редактор убирает обязательную bootstrap CLI-команду для уже enrolled device, но не получает seed/device/vault secrets и не смешивает конфигурацию с account/device enrollment или authority. |
| D-124 | 2026-09-04 | принято | Новый recoverable Account Root детерминированно получается из 256-bit entropy, представленной 24-word English BIP39 phrase, через отдельный BLAKE3 derivation domain. | Phrase с checksum даёт переносимое человекочитаемое представление 256-bit root authority; domain separation не позволяет переиспользовать BIP39 seed как другой Kilogram key. |
| D-125 | 2026-09-04 | принято | Root key хранится в bounded versioned platform envelope: Windows использует DPAPI CurrentUser, прежний raw 32-byte key атомарно мигрирует, non-Windows пока явно маркируется plaintext-development. | Account Root не должен оставаться raw рядом с уже защищённым device vault; typed provider/migration metadata исключает ложное обещание одинаковой защиты на всех ОС. |
| D-126 | 2026-09-04 | принято | Создание нового account и первого device выполняет отдельный one-shot `kilogram-bootstrap`: same-parent staging, certificate/device-list/prekey generation, vault migration/verify, затем один no-clobber directory rename. | Обычный runtime и его IPC не должны получать root/seed/device/vault secrets; частично созданный account не должен выглядеть готовым после сбоя. Persistent receipt содержит только public metadata, phrase передаётся GUI один раз в redacted/zeroizing bounded response. |
| D-127 | 2026-09-04 | принято | Phrase пока не разрешает создать пустой replacement Account Root: безопасный restore и enrollment второго device требуют authenticated полной authority history и отдельной транзакционной церемонии. | Один и тот же root key с обнулёнными sequence/revocations/device-list создаст валидно подписанный rollback/fork. M0.9.16 честно кодирует key, но не объявляет seed-alone account recovery. |
| D-128 | 2026-09-04 | принято | Existing-account link начинается с short-lived request, подписанного новым device и содержащего exact Account ID, HPKE public key, signed prekey pool и nonce; 12-digit SAS получается из digest всего signed request. | Root не должен принимать непроверенный public key или превращать знание seed/account ID в enrollment bearer; ручное сравнение связывает физическую церемонию с конкретным новым device. |
| D-129 | 2026-09-04 | принято | Root enrollment сериализуется OS lock, идемпотентен по exact Device ID/key/capabilities и становится действительным только после atomic публикации нового полного Root-signed device list. | Выданный, но не опубликованный certificate не авторизует device. Сбой может оставить monotonic sequence gap, но не частичный membership, rollback или повторную authority revision. |
| D-130 | 2026-09-04 | принято | Device-link authorization Root-signs exact request/certificate/list и HPKE-шифруется на encryption key нового device; accept читает identity только из DB-primary vault и коммитит trust через vault-primary transaction. | Public transfer medium не должен читать authority payload или перенаправить authorization другому device; установка не должна возвращать plaintext identity shadow либо обходить зарегистрированную trust mutation. |
| D-131 | 2026-09-04 | принято | Device link не объявляет историю полной: после enrollment новый device использует отдельные recipient-bound resumable recovery plans для каждого source и существующий signed claim reconciliation. | Authority enrollment и completeness истории — разные утверждения. Несколько sources повышают доступность и выявляют divergence, но даже согласие наблюдавшихся sources не доказывает глобальную полноту. |
| D-132 | 2026-09-04 | принято | M0.9.18 desktop только оркестрирует sibling bootstrap/CLI processes, требует stopped runtime для enrollment/recovery state operations и принимает bounded strictly validated output, привязанный к точным input/output paths. | GUI не должен получать Root/device/vault keys или становиться вторым state writer. Проверенный request нельзя подменить сменой path перед authorize; старые peer settings очищаются после accept. |
| D-133 | 2026-09-04 | принято | Multi-source recovery остаётся набором независимых recipient-signed plan files; GUI запускает одну bounded attempt, показывает terminal scheduler progress и отдельно выводит `incomplete`/`single-source`/`agreed`/`divergent` вместе с неизменным `global_completeness_proven=false`. | UI-список не является authority и не должен сливать несколько consent scopes в неявный общий план. Structured policy-blocked/scheduled outcome полезнее opaque process error, но background service и Task Scheduler этим этапом не создаются. |
| D-134 | 2026-09-04 | принято | Portable Account Root recovery package Root-подписывает current authority snapshot, complete device list и canonical set всех current conversation-membership heads; отдельный Root-подписанный witness фиксирует exact package digest и authority revision. | Phrase восстанавливает только signing key. Без sequence/revocations/list/membership heads тот же Root сможет создать корректно подписанный rollback/fork; old package с latest independently retained witness должен fail closed. |
| D-135 | 2026-09-04 | принято | Root recovery разрешён только в новый path через same-parent staging, exact phrase/package/witness validation и повторное byte-exact построение authority views; phrase helper читает только из stdin, а Root key заново оборачивается local platform provider. | Ошибка или неверный input не должны частично создать или перезаписать Root. Package/witness не переносят device/history secrets; enrollment и multi-source history recovery остаются отдельными церемониями. Совместный rollback matching package+witness требует будущего monotonic service/current-device quorum. |
| D-136 | 2026-09-04 | принято | M0.9.20 desktop только оркестрирует one-shot Root recovery helper при остановленном runtime: export, strict offline inspect, explicit newest-witness confirmation и restore в новый Root. После успеха GUI лишь заполняет Account ID/Root path существующей device-link ceremony. | GUI не становится вторым Root writer и не смешивает recovery authority с enrollment/history. Cryptographic validity показывается отдельно от global freshness, а matching rollback package+witness остаётся явной границей. |
| D-137 | 2026-09-04 | принято | Desktop держит phrase в zeroizing/redacted state, удаляет её из UI после enqueue и передаёт bounded helper input только через stdin. Restore повторно получает inspected package ID/revision и helper сверяет их до создания staging. | Command line/process diagnostics не должны раскрывать phrase, а canonical path недостаточен против valid same-path replacement между inspect и restore. Это memory hygiene и TOCTOU binding, но не защита от скомпрометированного GUI/OS input stack. |
| D-138 | 2026-09-04 | принято | Каждый успешный Account Root recovery export после внешней no-clobber публикации повторно захватывает authority lock, пересобирает exact package и лишь при полном совпадении атомарно сохраняет локальный Root-signed witness receipt. Status всегда пересобирает current package и сравнивает exact package ID/revision. | Любая authority или membership mutation становится `update-required`, включая изменение membership при той же authority revision. Receipt не содержит secrets и помогает lifecycle, но rollback всего Root откатывает и его, поэтому это не global freshness proof. |
| D-139 | 2026-09-04 | принято | Более сильный serverless recovery использует fresh 256-bit challenge и distinct device approvals одного exact package/state-vector/recovery-roster digest; каждый approver сначала проверяет DB-primary high-water state и атомарно сохраняет anti-equivocation head. Порог — strict majority exact active recovery roster. | Majority claim остаётся observation scope, пока recovery roster меняется одной Root signature. Для cross-roster fork-safety нужен отдельный recovery-policy epoch и joint-majority transition старого и нового roster; offline phrase+package+witness остаётся явным reduced-assurance fallback. |
| D-140 | 2026-09-04 | принято | M0.9.22 approval публикуется только после одной trust transaction, которая из DB-primary baseline проверяет и продвигает authority/membership high-water и сохраняет signed `recovery-approval/latest.approval`; same Request ID возвращает exact committed bytes. | Сетевой сбой или output failure не должен выпускать незаписанную подпись и позволять device одобрить конфликтующий fork. Exclusive state lock сериализует попытки, а trust classification включает head в authenticated vault delta/recovery. |
| D-141 | 2026-09-04 | принято | До recovery-policy epochs первый approval head закрепляет exact Root-signed roster digest; любые последующие изменения roster fail closed. Quorum verifier считает только distinct approvals exact request/challenge/package/roster и отдельно сообщает integrity-only, single-device или majority observation. | Majority intersection доказуема внутри одного roster, но не между произвольно Root-подписанными roster. Консервативная блокировка безопаснее ложного cross-roster claim; legit enrollment/revocation потребует joint old/new-majority transition. |
| D-142 | 2026-09-04 | принято | Network quorum использует device-signed one-shot `.kart`, который связывает exact request/expiry/certificate, Iroh endpoint, bearer capability и route policy; approval head коммитится до публикации ticket, а collector повторно проверяет `.kara` неизменным verifier. | Recovery client ещё не enrolled device, поэтому взаимная account-authentication невозможна: signed ticket является явным краткоживущим consent. Его кража допускает metadata exposure/DoS одной попытки, но не forge approval, secrets access или reuse для другого request. |
| D-143 | 2026-09-04 | принято | Desktop показывает literal quorum claim/threshold и разрешает Root restore либо после exact-package majority, либо после отдельного explicit reduced-assurance offline confirmation. | Недоступность current devices не должна незаметно превращаться в ложное freshness утверждение; GUI остаётся оркестратором bounded helper processes и не получает device/Root/vault secrets. |
| D-144 | 2026-09-04 | принято | Recovery electorate меняется только переходом policy epoch `N -> N+1`, подписанным strict majority старого и strict majority нового exact roster. Overlap-подпись считается в обоих порогах, new-only подпись — только в новом. | Одна Root/seed подпись не может заменить recovery voters. Joint consensus сохраняет пересечение доверия между roster, но потеря старого большинства намеренно делает безопасную смену состава невозможной без отдельного reduced-assurance процесса. |
| D-145 | 2026-09-04 | принято | Каждый voter до публикации `.karpa` коммитит DB-primary anti-equivocation head; policy anchor хранит Account ID, epoch, roster digest и previous transition ID. `.karpc` канонически объединяет distinct approvals и устанавливается одной trust transaction только active new-roster device. | Старый recovery approval head принимает новый roster лишь если установленный сертификат byte-exact связывает его old digest с candidate new digest. Сбой публикации или повторный conflicting request не должен выпускать незаписанную подпись. |
| D-146 | 2026-09-04 | принято | `cross_roster_fork_safety=true` выдаётся только совместно: валидный joint-majority `.karpc`, candidate package как monotonic successor certified new package и обычный strict majority exact candidate roster. | Сам Root-signed package, transport path, одиночный voter или partial joint quorum не повышают claim. Безопасность предполагает honest intersection и сохранность DB-primary signer heads; network/desktop orchestration transition остаётся следующим этапом. |
| D-147 | 2026-09-04 | принято | Permanent device removal выполняется Root-операцией `revocation + complete active-device list`; revision mismatch между этими durable writes блокирует recovery export, а exact retry использует тот же revocation и завершает list publication. | Файловая система не даёт общей atomic transaction для двух authority views. Fail-closed промежуток и идемпотентный repair не допускают использования stale roster и не расходуют повторный authority sequence. Последнее active device удалить нельзя. |
| D-148 | 2026-09-04 | принято | Removal helper требует byte-exact independently witnessed before checkpoint, доказывает exact `N -> N+1` successor и публикует public revocation/list/after checkpoint; desktop автоматически передаёт before/after в joint policy workflow, но пять lifecycle states остаются независимыми. | Root revocation немедленно меняет authority, но сама не устанавливает recovery policy, не обновляет работающий runtime, не retire ratchet sessions и не стирает уже доступную устройству историю. UI не должен объединять эти утверждения в один success flag. |
| D-149 | 2026-09-04 | принято | Live runtime принимает через authenticated IPC только Root-signed monotonic removal-only subset своего device list, одной vault-primary transaction устанавливает authority и retire revoked-device ratchet/prekey state, затем атомарно заменяет public ticket. | Работающий actor остаётся единственным state writer, downgrade/equivocation/addition fail closed, а повтор после ticket-publication failure безопасен. Новый fanout исключает revoked device после наблюдения ticket; уже подписанные recipient slots и скопированная история намеренно не переписываются и не объявляются удалёнными. |
| D-150 | 2026-09-04 | принято | Применённый live roster переживает restart через device-signed append-only receipt chain в vault-primary Runtime repository; stale launch profile не является fallback. Desktop может изменить только exact старый `device_list_file` path после сверки receipt status, state dir, connected IPC и неизменности profile. | Receipt атомарен с authority/ratchet mutation, тогда как public ticket и secret-free profile находятся вне vault. Поэтому crash после state commit восстанавливает новую ревизию и republish ticket; missing/tampered external list явно fail closed и repairable восстановлением exact файла, а runtime не получает general config write authority. |
| D-151 | 2026-09-05 | принято | M0.9.29 публикует runtime ticket не по открытому Account ID, а по domain-separated directional channel из conversation, publisher Account/Device и recipient Account. Каждый publisher Device имеет отдельный channel, одна публикация адресуется всем active recipient devices. | Store не получает явный social graph, а endpoints одного multi-device account не перезаписывают друг друга. Channel только pseudonymous: знающий все inputs может его вычислить, а traffic timing/size/IP остаются видимыми. |
| D-152 | 2026-09-05 | принято | Publisher хранит device-signed expiring hash chain ticket heads, HPKE-seals каждый head отдельными slots на complete verified recipient roster и загружает только opaque envelope. Recipient после exact contact/ticket/trust проверки коммитит device-signed observation high-water до atomic descriptor replacement. | Untrusted service не может прочитать или подделать ticket. Local chain предотвращает accidental publisher fork, observation отклоняет replay/equivocation после более нового локального head, а exact retry остаётся идемпотентным. First observation, cross-device fork detection и service availability не получают ложной гарантии. |
| D-153 | 2026-09-05 | принято | Wide-area adapter принадлежит единственному runtime actor через IPC v7; network wait разделён от коротких state/vault lock phases. HTTPS обязателен кроме numeric loopback, redirects и unbounded bodies запрещены; GUI даёт только explicit publish/refresh для already-enrolled contact. | Медленный/враждебный store не должен блокировать весь local profile, GUI не становится state writer и этап не включает скрытый background scheduler. Initial contact ticket всё ещё требует независимой проверки, а production service и abuse controls вынесены в следующий этап. |
| D-154 | 2026-09-05 | принято | `kilogram-ticket-store` является отдельным dependency-light workspace app без identity/protocol/ratchet/runtime crates и хранит в Redb только pseudonymous channel, declared generation, service timestamps и opaque bytes. Channel count и combined opaque body bytes учитываются transactionally. | Сервер не становится участником E2EE trust model и технически не может разобрать ticket. Generation header нужен только для atomic conditional replacement; он не аутентифицирован и не создаёт ложной availability guarantee. |
| D-155 | 2026-09-05 | принято | Store всегда слушает только loopback HTTP; публичный TLS, edge buffering и DDoS controls принадлежат reverse proxy. `X-Real-IP` учитывается только при явном `--trust-x-real-ip`, а proxy обязан его перезаписывать. | Это не даёт случайно открыть cleartext service и разделяет компактный проверяемый storage core от быстро меняющейся TLS/hosting конфигурации. Без trusted header per-IP bucket видит только TCP peer, обычно proxy. |
| D-156 | 2026-09-05 | принято | Service retention фиксирован оператором и не продлевается exact retry/GET; monotonic PUT принимает любой больший generation, exact replay, но отклоняет lower и same-generation different body. | Store не читает encrypted client expiry и позволяет восстановиться после upload gaps, migration или удаления старого object. Знающий channel всё ещё может записать высокий generation и вызвать DoS; write capabilities остаются отдельной нерешённой задачей. |
| D-157 | 2026-09-05 | принято | Iroh endpoints внутри Rust test-harness всегда отключают relay, address lookup, portmapper и optional net-report probes, удаляют default wildcard transports и явно bind только `127.0.0.1:0`; production и отдельный release process smoke используют обычный network builder. | Cargo даёт test EXE dependency-derived имя `target/*/deps/kilogram_cli-<hash>.exe`, поэтому wildcard bind и SSDP gateway discovery создавали повторные Windows Firewall prompts. Unit regression не нуждается во входящих LAN/public соединениях и не должен требовать firewall exception; реальные transport paths продолжают проверяться стабильными release EXE. |
| D-158 | 2026-09-05 | принято | Automatic ticket exchange является opt-in signed per-contact runtime policy с независимыми Ethernet/Wi-Fi/mobile/unknown permissions и работает только пока уже запущен runtime process. | Адреса должны обновляться без ручного переноса файлов, но эта настройка не регистрирует Task Scheduler/service/autostart, не превращает device в relay и не заменяет verified first-contact bootstrap. |
| D-159 | 2026-09-05 | принято | Publish и refresh имеют отдельные device-signed append-only attempt chains с policy generation, bounded exponential backoff и expiry-derived next time; commit после network wait повторно требует exact current policy head. | Restart не должен вызывать retry storm, а изменение/disable policy во время запроса не должно позволять старому результату стать current. Near-expiry success получает minimum 30-second recheck. |
| D-160 | 2026-09-05 | принято | Automatic publisher всегда создаёт следующую signed ticket-publication generation; manual explicit replay может повторно использовать текущую generation. | HPKE seal randomized: повторное шифрование той же signed generation даёт другие opaque bytes и правильно считается store-ом same-generation equivocation (409), тогда как scheduler renewal должен быть monotonic replacement. |
| D-161 | 2026-09-05 | принято | Runtime polling использует persistent monotonic interval, который не пересоздаётся после IPC или connection events. | Частые GUI status reads иначе могли навсегда откладывать delivery/sync/automation tick. UI activity не должна влиять на сетевой progress единственного actor. |
| D-162 | 2026-09-05 | принято | M0.9.32 оставляет один полный signed head каждой runtime ticket chain, а device-signed checkpoint связывает точные key/generation/record ID, canonical digest удаляемого delta, cumulative history digest и previous checkpoint ID. | Один digest без retained signed head не позволяет безопасно создать следующий chain element; хранение полного ticket внутри checkpoint раздувало бы один record. Exact anchor сохраняет continuation и high-water при bounded размере. |
| D-163 | 2026-09-05 | принято | Любая ticket chain compact-ится после превышения восьми retained records; новый checkpoint и удаления prefix/старого checkpoint коммитятся одной vault-primary transaction, после чего snapshot перечитывается и проверяет exact current checkpoint ID. | Startup scan больше не растёт со временем, а crash не может оставить видимым checkpoint без соответствующих removals или removals без checkpoint. Chain count остаётся ограничен contacts/global bounds. |
| D-164 | 2026-09-05 | принято | State transaction разрешает deletion только через отдельную регистрацию существующего `Runtime` record с synced backup в transaction manifest; остальные append-only repositories и незарегистрированное удаление остаются запрещены. | Crash-safe filesystem rollback нужен до подготовки DB-primary removal delta, но generic state layer не должен сам считать любой hash или файл достаточным authorization для compaction. Семантику подписанного checkpoint проверяет sole runtime actor. |
| D-165 | 2026-09-05 | принято | Opaque ticket channel теперь является domain-separated BLAKE3 hash отдельного Ed25519 write key; connection ticket v10 переносит этот public key внутри существующей Device signature. | Store сам проверяет соответствие key/channel, поэтому нет registration/TOFU race и знающий channel не может подставить собственный key. Account ID, Device ID и conversation label серверу не раскрываются. |
| D-166 | 2026-09-05 | принято | Write capability детерминированно выводится BLAKE3 `derive_key` из protected Device signing seed и recipient Account ID, временные buffers zeroize-ятся, private capability нигде не хранится и не передаётся. | Runtime restart сохраняет тот же already-enrolled lookup channel без нового secret repository; разные peer accounts и publisher devices получают unlinkable keys. Компрометация Device secret требует обычного revocation/rebootstrap. |
| D-167 | 2026-09-05 | принято | Каждый PUT содержит Ed25519 proof по exact channel, big-endian generation, body length и BLAKE3 body digest; store проверяет proof до durable generation transaction, GET остаётся unauthenticated. | Exact replay остаётся безопасно идемпотентным, но proof нельзя перенести на другой body/generation/channel. Capability решает channel takeover, а не read privacy, traffic analysis, Sybil capacity exhaustion или availability против malicious store. |
| D-168 | 2026-09-05 | принято | Multi-device endpoint set расширяет stable account+conversation contact отдельными local-device-signed append-only candidate records; один peer Device ID имеет один canonical descriptor path/policy, общий предел — четыре endpoint-а. | Existing queue и automation связаны со stable contact ID и не требуют миграции; локальный tamper/path substitution обнаруживается, а один send остаётся bounded. |
| D-169 | 2026-09-05 | принято | Endpoint resolver выбирает maximum readable authority revision, требует byte-exact snapshot при равной revision и использует только её complete prekey directory для единственной materialization. Older signed endpoint можно dial-ить для delivery лишь пока exact certificate остаётся active в newest roster; sync допускает только current-revision ticket. | Availability failover не должен устанавливать old authority/prekey state, возвращать revoked device или переписывать immutable event. Durable peer-authority pin остаётся окончательным локальным high-water. |
| D-170 | 2026-09-05 | принято | Delivery последовательно пробует deterministic order `authority revision desc, primary first, Device ID asc`, требует ack от exact attempted Device и после полного bounded candidate set создаёт только один signed retry generation. | Offline/invalid endpoint не блокирует другой peer device, одна queue item не материализуется повторно и failed candidate не создаёт отдельный retry storm. |
| D-171 | 2026-09-05 | принято | Одна contact refresh action обязана независимо обработать все enrolled peer Device channels в snapshot, но максимум четыре; HTTP GET выполняются параллельно, а trust/ratchet/observation/descriptor transactions строго последовательно. | Последовательные 20-second HTTP deadlines не помещаются в local IPC deadline, а параллельные state commits создают lock race. Разделение фаз сохраняет bounded latency и single-writer consistency. |
| D-172 | 2026-09-05 | принято | Успех каждого candidate фиксируется сразу и возвращается typed IPC result; aggregate `complete` истинен только при успехе всех candidates. Partial result не откатывает успешные high-water/descriptor commits, но automation записывает один failure и bounded backoff. | Availability одного Device не должна зависеть от другого, однако scheduler не должен маскировать stale endpoint как полный успех или создавать отдельную retry chain на каждый endpoint. |
| D-173 | 2026-09-05 | принято | IPC v10 определяет endpoint `usable` только для valid descriptor на newest byte-consistent authority revision с active exact certificate; остальные candidates явно `stale`. Read model показывает отдельный local publication high-water каждого известного channel. | Enrolled count сам по себе не сообщает реальную доступность. Typed состояние позволяет desktop честно показать missing/expired/old/revoked/equivocating candidate без превращения UI в новый источник trust. |
| D-174 | 2026-09-05 | принято | Каждый enrolled endpoint получает отдельный local-device-signed vault-primary publication binding по deterministic local Account/contact/peer Device ID; запись pin-ит peer Account/Device, conversation, route/path contract и self-authenticating public write key, но не transport address/prekey. | Lookup channel должен переживать prekey expiry, не превращаясь в бессрочный endpoint credential. Binding разрешает только discovery свежей публикации и не участвует в dial, sync, ratchet fanout или authority proof. |
| D-175 | 2026-09-05 | принято | Legacy enrollment без binding может один раз извлечь его из exact enrolled ticket после полной проверки всех signatures/Root identity/contract полей с пропуском только current-time prekey validity; binding коммитится до GET. Fetched ticket и любой usable descriptor обязаны сохранить exact pinned write key и пройти normal fresh verification. | Long-offline M0.9.35 contact восстанавливается без нового bootstrap, но expired ticket никогда не становится usable. Missing/tampered legacy descriptor fail closed, а publisher не может молча перевести existing contact на другой lookup capability. |
| D-176 | 2026-09-05 | принято | Endpoint announcement переносит exact current Root-signed own roster, bounded candidates/bindings и source-signed observation heads внутри source-Device-signed plaintext, HPKE-sealed exact recipient Device; import требует byte-equal local roster и не устанавливает authority/membership из bundle. | Уже авторизованные Devices могут делиться availability state без копирования local records/paths и без превращения file/gossip/store в Root authority. Revoked/stale source и wrong recipient fail closed. |
| D-177 | 2026-09-05 | принято | Recipient materializes собственные signed enrollment/binding records, а source observation сохраняет внутри отдельного local-device-signed acceptance evidence; highest sibling generation является anti-rollback lower bound, same-generation mismatch/equivocation блокируется. | Local observation chain остаётся contiguous и device-local, но long-offline Device не принимает replay старее уже увиденного sibling. Historical evidence не стирается отзывом source; automatic transport и bounded compaction остаются отдельными этапами. |
| D-178 | 2026-09-05 | принято | IPC v12 network push принимает только fresh recipient ticket с same listener/requester Account и byte-exact current own roster; source отправляет неизменённый M0.9.37 recipient-HPKE envelope по обычной Root/session-authenticated Device session, а recipient требует совпадения signed bundle source с authenticated requester и вызывает единственный общий import gate. | Network handler не становится вторым trust/writer path, sender не выбирает local descriptor paths и bundle по-прежнему не устанавливает authority/membership. Текущее single-audience ticket ограничение требует отдельного own-account recipient ticket до будущего multi-audience listener. |
| D-179 | 2026-09-05 | принято | Успешный import подтверждается recipient-Device signature по bundle ID, source/recipient, exact authority revision, result counts и текущему session binding; wire ALPN v8 допускает один envelope до 7 MiB и один ACK до 4 KiB на connection, runtime actor сериализует IPC pushes. | Exact bundle replay безопасно повторяет idempotent import и получает новый session-bound ACK, но captured ACK нельзя применить в другой сессии. Bounded one-shot exchange не является mailbox, gossip, OS background service или durable restart retry. |
| D-180 | 2026-09-05 | принято | Long-lived runtime использует один Iroh endpoint, но публикует два Device-signed ticket view: обычный для exact configured peer Account и stable own-device ticket для собственного Account; inbound authorization принимает объединение только этих аудиторий. | Peer messaging и own-device availability exchange больше не требуют перезапуска listener с другим requester. Третий Account остаётся запрещён, same-account Device обязан предъявить byte-exact current Root authority, а one-shot `listen` сохраняет single-audience contract. |
| D-181 | 2026-09-05 | принято | Own-device ticket атомарно публикуется рядом с IPC/primary ticket под полным Device ID и переиздаётся после live device-directory update; secret/IPC token в нём отсутствуют. | Стабильный внешний путь может синхронизироваться или позже заменяться discovery-каналом, не попадая в protected vault и не меняя endpoint. Отдельный requester-specific write capability остаётся под ticket signature. |
| D-182 | 2026-09-05 | принято | IPC v13 opt-in policy на recipient Device является Device-signed append-only chain и pin-ит canonical external ticket path, interval/validity/retry bounds и отдельные Ethernet/Wi-Fi/mobile/unknown permissions; одинаковая конфигурация idempotent. | Scheduler переживает restart без скрытого фонового сервиса, mobile/unknown fail closed по умолчанию, а замена public ticket по стабильному пути не требует новой policy generation. |
| D-183 | 2026-09-05 | принято | Foreground actor не чаще одного раза за automation check выполняет due own-device push через единственный M0.9.38 import/ACK gate; success и failure образуют Device-signed attempt chain с interval либо bounded exponential backoff и учитываются общим outbound-action limit. | Нет параллельного fan-out/retry storm или второго writer path. Revoked recipient перестаёт планироваться, exact replay остаётся idempotent, OS autostart явно false. |
| D-184 | 2026-09-05 | принято | Own-device policy/attempt chains входят в общий vault-primary runtime snapshot, имеют global bound 4096 и compact-ятся после восьми records через новые anchors существующего Device-signed transactional checkpoint. | Долгоживущая foreground automation остаётся restart-verifiable и bounded; старые checkpoint enum discriminants сохранены, ticket/wire/announcement formats не меняются. |
| D-185 | 2026-09-05 | принято | Pairwise locator capability выводится из certified long-lived X25519 Device keys через BLAKE3 derive-key и связывается с protocol domain, Account ID, digest byte-exact current Root roster и направленными source/recipient Device IDs. | Оба authorized sibling независимо получают один directional secret, обратное направление unlinkable, а смена Root roster вращает каналы. Empty context и all-zero non-contributory X25519 result запрещены. |
| D-186 | 2026-09-05 | принято | Fresh own-device ticket публикуется в pairwise channel как source-Device-signed monotonic publication, HPKE-sealed только exact recipient; store получает существующий self-authenticating PUT proof и opaque ciphertext без явных Account/Device IDs. | Store не получает plaintext/authority, но видит IP, timing, size и повторное использование channel внутри roster epoch. Shared pairwise write capability допускает DoS/outer-head poisoning со стороны самого still-authorized recipient, но не подделку source signature. |
| D-187 | 2026-09-05 | принято | Recipient принимает locator только после exact local HPKE open, source/Account/current-roster/listener/requester/freshness gates и signed observation anti-rollback/equivocation; verified ticket атомарно заменяется в deterministic runtime-managed public path. | Общая папка и ручное копирование больше не нужны, но путь не является trust anchor, discovery не enrol-ит Device и Root roster остаётся единственной authority. |
| D-188 | 2026-09-05 | принято | IPC v14 per-recipient discovery policy атомарно создаётся вместе с M0.9.39 announcement policy; один foreground action последовательно выполняет publish, fetch и existing authenticated push, использует общий limit/backoff и входит в signed checkpoint compaction. | Нет hidden worker, parallel fan-out, OS autostart или mailbox. Network policy/revocation fail closed, а одинаковая configuration idempotent. |
| D-189 | 2026-09-05 | принято | Fresh locator publication переиспользуется без PUT только после signed успешного полного workflow; после любого incomplete attempt создаётся следующая signed generation и новый HPKE envelope. | HPKE randomized: повторное шифрование той же generation даёт другой body и обязано получать store `409` как equivocation. Generation advance делает retry корректным даже при неопределённом HTTP timeout, когда предыдущий PUT мог фактически сохраниться. |
| D-190 | 2026-09-05 | принято | Одна local-Device-signed roster policy связывает operator settings с exact Root authority revision и canonical device-list digest и transactionally проецируется в существующие pairwise discovery/announcement child chains для каждого sibling Device. | Root roster остаётся authority, GUI/store не получают права enrol/revoke, одинаковая configuration idempotent, а смена roster всегда оставляет authenticated parent/child generation и вращает pairwise channels. |
| D-191 | 2026-09-05 | принято | Reconciliation хранит union active и historical recipients: active получают enabled configuration, отсутствующие после Root revocation — disabled generation. Expanded complete launch roster добавляется на startup; existing live update остаётся removal-only и не обходит fresh-prekey/profile convergence gate. | Отзыв прекращает foreground work немедленно и остаётся аудируемым. Hot enrollment не объявляется реализованным без полного prekey directory; manual per-recipient mutation после global policy fail closed, чтобы не было двух configuration authorities. |
| D-192 | 2026-09-05 | принято | IPC v15 конфигурирует и показывает roster-wide policy, а actor выбирает максимум один due child в deterministic order и завершает publish/fetch/push до следующего. Parent chain имеет 1024-record defensive bound и новый append-only checkpoint anchor. | Большой roster не создаёт parallel fan-out внутри процесса; status явно ограничивает гарантию foreground runtime scope, не притворяясь distributed mutex между машинами или OS background service. Existing wire/ticket/ratchet formats не меняются. |
| D-193 | 2026-09-05 | принято | Endpoint-announcement bundle v2 несёт для каждого channel максимум один higher direct либо local-Device-signed accepted observation; forwarded acceptance сохраняет original observer signature, а получатель создаёт собственную acceptance для следующего hop. | High-water сходится по A -> B -> C без shared folder и без объявления store глобальным witness. Bundle source обязан быть acceptance witness в exact current roster; одинаковый publication tuple повторно не записывается. |
| D-194 | 2026-09-05 | принято | Import объединяет direct/accepted claims до mutation и полностью отклоняет различающиеся publication ID/digest одной channel generation; `.aeo` sets compact-ятся до одного highest signed record с exact checkpoint anchor. | Authenticated equivocation нельзя молча выбрать по arrival order. Local high-water остаётся monotonic и bounded, но конфликт известен только встретившему обе подписи Device; durable quarantine/UX и внешний completeness witness остаются отдельными задачами. |
| D-195 | 2026-09-05 | принято | Первый same-generation publication mismatch сохраняется как один append-only `.pcf`: local detector Device подписывает canonical pair исходных Device-signed observations, channel/generation и время; content-addressed proof reload-ится только с exact local identity и matching durable endpoint binding. | Ошибка переживает restart и не зависит от arrival order. Это доказательство противоречивых Account-Device assertions у одного detector, а не доказательство двух peer-publisher signatures и не global consensus; compromised Device способен вызвать fail-closed DoS. |
| D-196 | 2026-09-05 | принято | Наличие `.pcf` карантинит только соответствующий publication channel до будущей явной Root-authorized re-enrollment/rotation ceremony: delivery, auto-sync и HTTP refresh исключают endpoint до network I/O и повторно проверяют quarantine после fetch. Delete/override API отсутствует, store и GUI не могут снять stop. | Restart или newer claim не стирают security evidence. Healthy sibling endpoint разрешён только при peer authority не ниже local pinned high-water и exact bytes при equal revision, поэтому quarantine нового endpoint не реактивирует старый revoked roster. |
| D-197 | 2026-09-05 | принято | IPC v16 добавляет endpoint state `quarantined`, proof ID, conflict generation/detection time и отдельный count; Windows UI показывает красное audit/re-enrollment состояние. Network import после успешной proof persistence отвечает rejection, но не останавливает long-lived runtime и публикует IPC change. | Обычные import/vault failures по-прежнему останавливают runtime fail closed. Mixed contact продолжает refresh healthy channels; automation перестаёт retry только когда quarantined все publication channels. Proof propagation и безопасное снятие quarantine остаются следующим этапом. |
| D-198 | 2026-09-06 | принято | Endpoint-announcement bundle v3 переносит вместо ordinary observation полный detector-Device-signed conflict proof; detector обязан быть active в exact-current Root roster, recipient проверяет canonical pair и сохраняет собственный Device-signed `.pcf`. | Чужой local proof не становится локальной authority записью, но quarantine сходится A -> B -> C. Compromised active Device всё ещё способен вызвать DoS; proof не объявляется peer-equivocation proof. |
| D-199 | 2026-09-06 | принято | Detector/time-specific proof ID дополнен stable evidence ID от canonical пары полных signed observations. Root resolution связывает evidence ID, а не local proof ID. | Один audited Root artifact можно применить на всех current siblings, которые независимо re-sign одни и те же evidence; их local provenance и `.pcf` filenames остаются разными. |
| D-200 | 2026-09-06 | принято | Connection ticket v11 подписывает monotonic publication channel epoch; epoch 0 byte-semantically сохраняет исходную derivation, non-zero epoch использует отдельный domain. Local Device-signed `.pcrn` chain обязан быть contiguous, а runtime применяет новый epoch только после явной команды и restart. | Реальная channel rotation больше не переиздаёт прежнюю capability. Remote conflict report не получает права самостоятельно вращать peer channel или менять runtime ticket. |
| D-201 | 2026-09-06 | принято | Root-signed `.pcr` связывает exact local authority revision, stable evidence, peer Account/Device, old/new write keys и exact fresh replacement-ticket digest. Apply сохраняет old binding и `.pcf`, заменяет descriptor fail-closed и только затем transactionally включает effective key override; different second resolution запрещён. | Store/GUI/newer claim не могут снять quarantine. Crash между descriptor replace и resolution commit оставляет endpoint unusable до idempotent retry. Текущая online-state + offline-Root команда является M0 boundary и должна быть разделена request/response signer flow. |
| D-202 | 2026-09-06 | принято | Online runtime Device создаёт bounded Device-signed `.pcrq` с complete local proof, exact authority revision, old/new epoch/key/route summary и byte-exact peer replacement ticket; команда не принимает Account Root path. | Root secret не попадает в online request process. Любое изменение public evidence или replacement invalidates request ID/signature; compromised active Device всё ещё может запросить DoS, но не авторизовать его. |
| D-203 | 2026-09-06 | принято | Offline `account-publication-conflict-authorize` загружает только Root + `.pcrq`, требует active requester/detector в exact published roster и независимо проверяет embedded fresh peer ticket. Resolution v2 Root-подписывает request ID; self-contained `.pcrp` включает exact public ticket. | Signer не читает runtime state, а apply не читает Root. Request ID делает offline audit адресным; resolution security по-прежнему прямо bind-ит stable evidence, peer, keys, digest и revision. |
| D-204 | 2026-09-06 | принято | Bundle v4 может переносить Root resolution только рядом с exact digest-matching replacement endpoint. Recipient обязан уже иметь тот же old binding и local `.pcf` с stable evidence ID; descriptor replace выполняется до transactional `.pcr` append. | Resolution не bootstrap-ит контакт/authority и не снимает чужой quarantine. C -> A -> B convergence использует существующий recipient-HPKE channel и сохраняет fail-closed crash gap; ACK v2/IPC v18 считают resolution отдельно. |
| D-205 | 2026-09-06 | принято | Windows incident UI создаёт `.pcrq`, показывает явную offline signer CLI-команду и применяет `.pcrp` только при остановленном runtime; Root directory/input в UI отсутствует. | Desktop помогает оператору, не превращаясь в Root signer. Hardened offline evidence viewer/QR appliance и live peer-side rotation остаются следующими слоями. |
| D-206 | 2026-09-06 | принято | Authenticated IPC v19 и один serialized runtime actor владеют live rotation собственного publication channel, созданием `.pcrq` и применением `.pcrp`; Root signing остаётся отдельной offline CLI-командой. | GUI не открывает `STATE_DIR` и не получает Root secret, а messaging endpoint продолжает работать во время incident response. |
| D-207 | 2026-09-06 | принято | Live rotation сначала transactionally фиксирует signed epoch, затем атомарно заменяет configured public ticket и только после этого меняет actor ticket; retry переиздаёт durable pending epoch как `AlreadyPresent`. | Crash между commit и publish остаётся fail-closed и восстанавливается без пропуска epochs или повторной rotation. Fresh ticket всё ещё должен дойти до peer. |
| D-208 | 2026-09-06 | принято | Windows показывает единый incident lifecycle, но request/apply progress хранит только в UI session; durable truth остаётся в `.pcf`, переносимом `.pcrq` и применённом `.pcr`. | Клиент не изображает распределённый workflow coordinator. Все live reports явно сообщают `runtime_restart_required=false`, а offline Root boundary остаётся видимой оператору. |
| D-209 | 2026-09-06 | принято | Полные `.pcrq`/`.pcrp` остаются bounded signed artifacts на removable media; QR содержит только versioned compact public claim с artifact/request IDs, account/revision, evidence, peer, channels, ticket digest и KPC1 code. | Multi-MiB authority/evidence не маскируется под якобы удобный QR. QR полезен только при exact match signed artifact и независимом канале; request/response claims имеют разные kind/artifact ID. |
| D-210 | 2026-09-06 | принято | KPC1 — domain-separated length-prefixed 96-bit BLAKE3 commitment common security fields request/response; offline authorize обязан сравнить explicit `--confirm-code` после public artifact/QR inspection, но до resolve/load Account Root. | Blind file-in/file-out Root signing исключён из штатного CLI. Код не заменяет подписи и не аутентифицирует peer; при одном скомпрометированном канале совпадение не даёт независимого trust. |
| D-211 | 2026-09-06 | принято | Public inspectors работают без Root/runtime state, bounded-проверяют complete evidence и embedded ticket; request/response вне Root directory, output no-clobber. IPC v20 передаёт GUI только request digest/code и public metadata. | Root остаётся вне desktop/live actor. Общий `kilogram-cli` всё ещё имеет больший TCB, чем purpose-built offline signer, поэтому минимальный отдельный binary/package остаётся следующим hardening этапом. |
| D-212 | 2026-09-06 | принято | `kilogram-offline` имеет только `inspect-request`, `authorize`, `inspect-response`; dependency gate запрещает Iroh/Tokio/Reqwest и Kilogram runtime/protocol/session/state/store/transport crates. | Offline Root host не получает live messaging/network command surface. `kilogram-identity`, ratchet public directory verification, image/QR и format codecs всё ещё входят в TCB. |
| D-213 | 2026-09-06 | принято | Offline v11 ticket verifier сохраняет raw endpoint JSON для byte-exact Device signature reserialization и отдельно bounded-проверяет его public summary; cross-component regression обязан пропускать online request -> offline response -> online apply. | Wire format не меняется и Iroh parser не попадает на Root host. Дублирование artifact codec между CLI и offline binary остаётся maintenance-риском, который M0.9.49 должен устранить общим minimal crate. |
| D-214 | 2026-09-06 | принято | Portable offline package строится `cargo --locked` только из clean worktree, не перезаписывает output, включает stable-name EXE, operator README, source/toolchain provenance и SHA-256 manifest; ZIP имеет фиксированный порядок/timestamp. | Упаковка детерминирована для одинаковых payloads, но это ещё не независимо воспроизводимая supply-chain сборка: требуются pinned builder image, второй builder и подписанный release provenance. |
| D-215 | 2026-09-06 | принято | На Windows network-bearing test harness нельзя запускать непосредственно через `cargo test`: обязательный local workflow сначала делает `--no-run`, копирует каждый hash-suffixed artifact рядом под детерминированным стабильным именем и запускает его только при явном `-Run`. | Даже loopback listener способен вызвать Firewall approval, а правило Windows привязано к пути EXE. D-157 остаётся полезным ограничением сетевой экспозиции, но его предположение об отсутствии prompt неполно; stable path устраняет повторные запросы после единственного осознанного разрешения. |
| D-216 | 2026-09-06 | принято | Вся подписанная publication-conflict chain от ticket publication/observation до proof, Device request, Root response и KPC1 claim имеет одного владельца — network-free `kilogram-publication-conflict`; online/offline приложения используют только его codecs/verifiers. | Proof/evidence IDs включают exact serialized observations, поэтому частичное выделение только request/response оставило бы критичное дублирование ниже trust boundary. Один implementation исключает field/domain/version drift. |
| D-217 | 2026-09-06 | принято | Signed artifacts используют transport-independent `PublicationConflictRoutePolicy` с прежними variant order и kebab-case wire names; online transport переводит собственный `RoutePolicy` явным exhaustive match. | Минимальный codec не должен зависеть от Iroh, но wire bytes должны остаться совместимыми. Явный adapter делает будущие transport semantics заметным изменением, а не неявным импортом runtime types. |
| D-218 | 2026-09-06 | принято | Locked boundary gate запрещает network/runtime/image dependencies общего codec и source-scan отклоняет повторное объявление критичных wire structs/KPC1 domain вне crate; offline gate требует этот shared owner. | Архитектурная граница должна проверяться машиной, иначе удобная локальная копия быстро вернёт несовместимые codecs или расширит Root-host TCB. |
| D-219 | 2026-09-06 | принято | Обычный incremental `cargo check` остаётся быстрым, а full test-harness compile использует отдельный `verification` profile с line tables и без incremental; stable harness workflow по умолчанию только компилирует. Heavy project scripts наследуют BelowNormal и используют половину logical processors, с explicit override. | Главные 100+ GiB источники — многопоколенный test incremental и full PDB — больше не смешиваются с dev cache. Сетевые harness EXE не стартуют без explicit `-Run`; cold rebuild не должен забирать отзывчивость Windows/VMware ценой умеренного увеличения времени. |
| D-220 | 2026-09-06 | принято | Offline reproducibility gate копирует один SHA-256-manifested clean Git source set в два разных source roots, использует два target roots, frozen Cargo, pinned Rust target, disabled incremental, path remap, commit epoch и `/Brepro`, затем требует равенство длины и SHA-256 EXE. | Это доказательство `same-host-separate-clean-roots`, а не независимый второй builder. Запись обязана честно фиксировать этот scope и оба artifacts. |
| D-221 | 2026-09-06 | принято | Clean `kilogram-offline` package fail closed требует проверенный reproducibility record exact текущего HEAD; package включает record, source manifest, Cargo.lock и rust-toolchain, а dirty development escape hatch явно помечается `reproducibility_verified=false`. | Обычный release больше не может незаметно упаковать единственный локальный binary. SHA256SUMS не заменяет authenticated distribution или signed provenance. |
| D-222 | 2026-09-06 | принято | Cargo cache maintenance read-only по умолчанию, автоматически вызывается stable harness workflow, предупреждает после 40 GiB и удаляет только по явному `-PruneVerification` либо guarded `-VacuumAndWarm`; destructive target обязан быть exact workspace child. | Автоматическая фоновая очистка не должна неожиданно отнимать build cache и время. Периодический размер видим, disposable verification отделён, а dev/release outputs сохраняются до осознанного обслуживания. |
| D-223 | 2026-09-06 | принято | Blind mailbox address содержит независимые Ed25519 read/write public keys; Mailbox ID — domain-separated hash обоих keys, registration отсутствует. Recipient хранит read secret, а sender получает scoped write secret. | Знание публичного адреса не даёт list/delete/put authority, а storage namespace не содержит Account, Device, contact или conversation identifier. Capability distribution и rotation остаются responsibility client/contact protocol. |
| D-224 | 2026-09-06 | принято | Mailbox item имеет random 256-bit ID, а opaque payload HPKE-шифруется recipient key с AAD по exact mailbox/item/creation/expiry; protocol ограничивает TTL 60 секунд–7 дней, plaintext 512 KiB и envelope 1 MiB. | Storage node не получает readable event или application IDs, а ciphertext нельзя незаметно перенести под другой mailbox/item. Это не скрывает IP, timing, size или повторные обращения к одному pseudonymous mailbox. |
| D-225 | 2026-09-06 | принято | Store signing key pin-ится при первом открытии Redb. Accepted bytes получают signed stored receipt; exact put replay возвращает её же. Read-authorized delete conditional по receipt ID и оставляет signed tombstone до исходного expiry. | Receipt доказывает принятие exact bytes данным store key, но не будущую availability/replication. Tombstone блокирует delayed replay/resurrection и сохраняет idempotent delete; оператор всё ещё может удалить или удержать ciphertext. |
| D-226 | 2026-09-06 | принято | `kilogram-mailbox` остаётся отдельным network-free durable contract crate с finite per-mailbox/global item/byte bounds; locked gate запрещает Iroh/Tokio/Reqwest/runtime/state/store/transport и source-scan-ом запрещает application identifier types. | HTTP/P2P и client adapters можно менять, не расширяя blind-store trust boundary. M0 cleanup/list используют bounded full scan и не заявлены scalable production index. |
| D-227 | 2026-09-06 | принято | До реального внешнего теста или выпуска обычные milestones не выполняют release-сборки и ZIP packaging; default evidence — resource-bounded debug `cargo check`, strict Clippy и unoptimized verification harness, network executable запускается только осознанно под stable path. | Оптимизированная линковка и пакеты не улучшают текущую protocol работу, но заметно расходуют CPU, диск и cache. Release/reproducibility machinery сохраняется и будет запускаться только когда artifact действительно нужен. |
| D-228 | 2026-09-06 | принято | Blind-mailbox wire использует versioned bounded Postcard request/response; list capability подписывает nonce, exclusive cursor и limit максимум 8 items, а каждый page receipt проверяется по pinned store key. | Один запрос не выгружает весь mailbox и не создаёт ответ порядка 128 MiB. Pagination пока использует bounded full scan и random item-ID order, а не production index. |
| D-229 | 2026-09-06 | принято | Mailbox HTTP routes добавлены в существующий loopback-only `kilogram-ticket-store` за тем же обязательным HTTPS reverse proxy; отдельный service EXE не создаётся. | Уменьшается operational и Firewall surface. Legacy имя binary теперь уже его двойной роли; разделение возможно позже без изменения network-free mailbox contract. |
| D-230 | 2026-09-06 | принято | Service store identity — отдельный exclusive-created Ed25519 secret рядом с data dir; symlink/wrong length запрещены, public key pin-ится Redb и печатается при startup. | Signed receipt переживает restart и обнаруживает смену identity. Windows file confidentiality пока полагается на inherited directory ACL; authenticated provisioning URL/store key реализован M0.9.53, stronger store key provider остаётся открыт. |
| D-231 | 2026-09-06 | принято | `kilogram-mailbox-client` допускает remote только HTTPS, запрещает redirects, bounded читает body и продвигает durable state только по positive receipt exact expected store key. | TLS аутентифицирует endpoint, store key — application receipt identity; они не заменяют друг друга. Negative response не является signed proof, потому что malicious storage всё равно способен withholding/DoS. |
| D-232 | 2026-09-06 | принято | Client ledger хранит exact encrypted pending request и signed receipt states без secret capabilities; conditional delete становится доступен только после caller-supplied stable application commit ID. Logical retries ignore новый observation timestamp, а expired records чистятся atomic transaction. | Crash/retry не теряет uncertain upload и не удаляет ciphertext до local durable ingestion. Ledger не может доказать flush чужой БД: runtime application commit обязан быть idempotent и transactionally authoritative. |
| D-233 | 2026-09-06 | принято | Mailbox-client boundary fail closed запрещает application IDs, Iroh и identity/protocol/ratchet/runtime/session/state/store/transport dependencies и новый binary; pure route test вызывается без socket bind. | Transport/ledger можно проверить без расширения runtime TCB и без нового Windows Firewall prompt. Live runtime wiring, capability distribution и actual external HTTPS test остаются отдельным этапом. |
| D-234 | 2026-09-06 | принято | Full per-contact/per-device mailbox read/write capability Device-подписана и HPKE-sealed к собственному Device; peer artifact содержит только write secret, подписан owner Device и HPKE-sealed к exact recipient certificate. Outer artifact раскрывает только version и opaque binding ID. | Write capability остаётся spam authority и не попадает в public ticket/endpoint publication/client ledger; read secret никогда не покидает owner. Untrusted file transport не читает Account/Device/service metadata, но видит стабильный opaque artifact и timing. |
| D-235 | 2026-09-06 | принято | Offer signature связывает owner/recipient Account+Device, owner encryption key, conversation scope, mailbox address, canonical HTTPS URL, pinned store key и bounded expiry. Import требует current enrolled peer endpoint и обновляет pinned Root authority high-water из authenticated descriptor. | Старый, чужой, cross-conversation, expired, revoked-device или tampered offer fail closed. Автоматическая online delivery/rotation offer остаётся следующим слоем. |
| D-236 | 2026-09-06 | принято | Local и imported mailbox bindings сохраняются append-only через existing state transaction/vault как локально Device-signed wrappers; общий limit 1024. Capability creation durable-commit-ит local secret до публикации no-clobber peer artifact, exact import replay идемпотентен. | Crash не должен оставить выданную peer write capability без retained read secret. Orphan local binding при crash до artifact export безопасен и bounded. Runtime до M0.9.54 обязан сообщать `provisioned-not-yet-enabled`, а не считать mailbox доставкой. |
| D-237 | 2026-09-06 | принято | Runtime пробует все current direct/relay endpoint candidates первым путём и только после bounded failure создаёт Device-signed mailbox dispatch. Item ID детерминирован по queue/event/binding, exact encrypted PUT сохраняется до HTTPS, default TTL 24 часа ограничивается expiry binding; orphan dispatch до ledger enqueue восстанавливается только после exact revalidation. | Mailbox не ухудшает normal live latency и crash/retry не создаёт несколько логических копий или permanent stuck queue. Expired/stale binding fail closed; store остаётся availability helper, а не delivery authority. |
| D-238 | 2026-09-06 | принято | Store-signed PUT receipt означает только `mailbox-stored`. `delivered` создаётся лишь peer-signed AuthorizedEvent acknowledgement. Inbound event/projection/ACK или acknowledgement/delivered marker коммитятся application transaction до ledger commit и conditional delete. | Storage node не может имитировать прочтение/приём peer. Crash до application commit оставляет item для replay, после commit — pending delete; idempotent application transaction закрывает двойное применение. |
| D-239 | 2026-09-06 | принято | Получатель durable-enqueue-ит peer-signed ACK в отдельно provisioned reverse mailbox; actor повторяет exact pending PUT. Без current reverse binding входной text fail closed не удаляется. | ACK не зависит от одновременной online-сессии, но удаление сообщения без доступного обратного канала не должно создавать ложную финальность. Mutual provisioning является явной предпосылкой asynchronous delivery. |
| D-240 | 2026-09-06 | принято | Mailbox интегрирован в единственный serialized runtime actor: live work имеет приоритет, затем не чаще одного mailbox действия за 5 секунд; signed page максимум 8 items, network wait вне state lock. IPC v21 отдельно показывает pending/stored/expired/failed и received/deleted ledger counts. | Нет второго client EXE, Task Scheduler или parallel state writer; slow/malicious service не блокирует короткие local transactions и не превращает storage receipt в delivered. Production scheduling/backoff/push остаются отдельным слоем. |
| D-241 | 2026-09-06 | принято | Mailbox capability lifecycle является Device-signed contiguous chain отдельно для exact owner/recipient Device и opaque conversation scope: generation one только activates, каждый следующий update указывает exact predecessor, revoke называет active binding, rotate обязан создать другой binding. | Retained state fail closed обнаруживает rollback, gap, fork, wrong revocation и повторное использование binding. Account Root не участвует в каждом update, но current Device authorization проверяется authenticated session и endpoint high-water. |
| D-242 | 2026-09-06 | принято | Recipient-HPKE offer передаётся только внутри existing mutually authenticated Device QUIC session; runtime не чаще одного раза за 30 секунд отправляет eligible unacknowledged update, generation N+1 ждёт durable ACK generation N, а in-memory oldest-attempt ordering не даёт одному offline contact блокировать остальные. | Capability secrets не попадают в public ticket/endpoint publication и retries сохраняют строгий per-chain порядок. Ошибка control transfer изолирована от runtime, нового EXE/Task Scheduler нет. |
| D-243 | 2026-09-06 | принято | Получатель atomic persist-ит imported peer binding и signed update до recipient-Device-signed ACK, привязанного к exact transport session/update/direction/generation/binding/action; sender сохраняет ACK append-only. | Storage или replay чужой сессии не могут объявить convergence. Expired historical activation разрешено crypto-import-ить на signed creation time только для продвижения chain; embedded expiry не меняется и делает binding unusable now. |
| D-244 | 2026-09-06 | принято | Runtime использует non-revoked capability-chain head; при одной unacknowledged rotation owner временно poll-ит также previous acknowledged active binding. Следующий rotate/revoke запрещён до ACK текущего head; старые records остаются append-only. IPC v22 показывает pending ACK и revoked heads. | Handover overlap не теряет сообщения peer, ещё использующего старый binding. Local revoke немедленно прекращает old poll, а peer revoke после получения прекращает fallback. Отзыв не стирает leaked write secret/opaque store data; остаток ограничивается неприменением old head, TTL и quotas. |

## Правило изменения решений

Изменение принятого решения должно сопровождаться новым ADR/RFC или явной
правкой этого журнала с причиной, последствиями и датой. Предварительные
решения становятся принятыми только после технической проверки.
